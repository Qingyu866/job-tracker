# 模拟面试前端流程顺序图

**创建日期**: 2026-03-17
**密级**: 内部公开
**对应文档**: `模拟面试系统前端设计_20260317.md`

---

## 一、实体关系说明

### 1.1 JobApplication 与 MockInterviewSession 的关系

```
┌──────────────────────┐         ┌──────────────────────────┐
│   JobApplication     │         │   MockInterviewSession    │
│   (岗位申请)          │         │   (模拟面试会话)           │
├──────────────────────┤         ├──────────────────────────┤
│ • id                 │<────┐   │ • id                     │
│ • company_id         │     │   │ • session_id             │
│ • job_title          │     │   │ • application_id  ───────┼───┐ (外键)
│ • status             │     │   │ • company                │   │
│ • application_date   │     │   │ • job_title              │   │
│ • resume_id          │     │   │ • resume_id              │   │
└──────────────────────┘     │   │ • status                 │   │
                             │   └──────────────────────────┘   │
                             │                                  │
┌──────────────────────┐     │   ┌──────────────────────────┐   │
│   Company            │     │   │   InterviewMessages       │   │
│   (公司信息)          │     │   │   (面试消息)              │   │
├──────────────────────┤     │   ├──────────────────────────┤   │
│ • id                 │────┼──>│ • id                     │   │
│ • name               │     │   │ • session_id      ───────┼───┘
│ • industry           │     │   │ • role                   │
│ • size               │     │   │ • content                │
└──────────────────────┘     │   └──────────────────────────┘
                             │
    关键关系说明：
    1. MockInterviewSession.application_id → JobApplication.id
    2. 从 JobApplication 获取：company、jobTitle、resumeId
    3. 一次 JobApplication 可以有多个 MockInterviewSession（可重复面试）
```

### 1.2 前端数据流

```
用户在职位列表/详情页
    ↓
选中一个 JobApplication (id=123)
    ↓
点击"模拟面试"按钮 (传递 applicationId=123)
    ↓
后端创建 MockInterviewSession (关联 application_id=123)
    ↓
从 JobApplication 获取公司名、职位名
    ↓
面试页面显示：字节跳动 - Java后端工程师
```

---

## 二、用户发起模拟面试完整流程

### 2.1 总体时序图

```
┌─────────────┐      ┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│   用户 UI    │      │   前端应用   │      │  后端 API     │      │ AI Agent系统 │
└──────┬──────┘      └──────┬──────┘      └──────┬───────┘      └──────┬──────┘
       │                    │                     │                      │
       │  1. 点击"模拟面试"   │                     │                      │
       │ ──────────────────>│                     │                      │
       │                    │                     │                      │
       │  2. 显示简历选择弹窗 │                     │                      │
       │ <─────────────────>│                     │                      │
       │                    │                     │                      │
       │  3. 选择简历并确认  │                     │                      │
       │ ──────────────────>│                     │                      │
       │                    │                     │                      │
       │                    │  4. POST /api/interview/sessions          │
       │                    │  {applicationId, resumeId}                │
       │                    │ ──────────────────────────────────────>   │
       │                    │                     │                      │
       │                    │                     │  5. 创建 MockInterviewSession
       │                    │                     │                      │
       │                    │                     │  6. 初始化三个 Agent │
       │                    │                     │ ───────────────────>│
       │                    │                     │                      │
       │                    │                     │  7. 返回 sessionId   │
       │                    │ <──────────────────────────────────────    │
       │                    │                     │                      │
       │  8. 跳转到面试页面   │                     │                      │
       │ ──────────────────>│                     │                      │
       │                    │                     │                      │
       │                    │  9. 导航到 /interview/:sessionId         │
       │                    │ ──────────────────>│                      │
       │                    │                     │                      │
       │                    │                     │  10. 加载会话详情   │
       │                    │                     │ <───────────────────│
       │                    │                     │                      │
       │  11. 显示面试界面   │                     │                      │
       │ <──────────────────│                     │                      │
       │                    │                     │                      │
       │  12. 发送第一条消息  │                     │                      │
       │ ──────────────────>│                     │                      │
       │                    │                     │                      │
       │                    │  13. POST /api/interview/chat            │
       │                    │ ──────────────────────────────────────>   │
       │                    │                     │                      │
       │                    │                     │  14. 三Agent协同    │
       │                    │                     │ ───────────────────>│
       │                    │                     │                      │
       │                    │                     │  15. 返回 AI 响应   │
       │                    │ <──────────────────────────────────────    │
       │                    │                     │                      │
       │  16. 显示 AI 回复   │                     │                      │
       │ <──────────────────│                     │                      │
```

---

## 三、详细流程说明

### 3.1 触发入口

**⚠️ 重要**: 模拟面试必须关联到具体的 JobApplication

**位置**: 职位详情页，选中某个 JobApplication 后触发

**位置**: 职位详情页 (`CompanyManagementPage.tsx` 或独立的 `JobDetailPage.tsx`)

```tsx
<Button
  onClick={() => setShowStartInterviewDialog(true)}
  className="interview-btn"
>
  <MessageSquare className="w-4 h-4" />
  模拟面试
</Button>
```

### 3.2 传递 applicationId

```tsx
// CompanyManagementPage.tsx 或 JobDetailPage.tsx
// 当前选中的岗位申请
const [selectedApplication, setSelectedApplication] = useState<JobApplication | null>(null);

// 点击"模拟面试"时，已明确知道是哪个申请
<Button
  onClick={() => {
    // 确保已选中申请
    if (!selectedApplication) {
      toast.error('请先选择一个岗位申请');
      return;
    }
    setShowStartInterviewDialog(true);
  }}
  className="interview-btn"
>
  <MessageSquare className="w-4 h-4" />
  模拟面试
</Button>
```

### 3.3 简历选择弹窗

```tsx
<Dialog open={showStartInterviewDialog} onOpenChange={setShowStartInterviewDialog}>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>选择面试简历</DialogTitle>
      <DialogDescription>
        请选择用于本次模拟面试的简历，AI 面试官将根据简历内容提问
      </DialogDescription>
    </DialogHeader>

    <ResumeList
      resumes={userResumes}
      onSelect={setSelectedResume}
    />

    <DialogFooter>
      <Button onClick={handleStartInterview}>
        开始面试
      </Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

### 3.4 创建面试会话 API

**⚠️ 关键**: applicationId 是必传参数，后端会根据它获取公司信息和职位名称

```typescript
// src/services/interviewApi.ts

export interface StartInterviewRequest {
  applicationId: number;
  resumeId: number;
}

export interface StartInterviewResponse {
  sessionId: string;
  company: string;
  jobTitle: string;
  status: 'IDLE' | 'WELCOME' | 'TECHNICAL_QA' | 'SCENARIO' | 'EVALUATION' | 'FINISHED';
  createdAt: string;
}

export async function startInterview(params: StartInterviewRequest): Promise<StartInterviewResponse> {
  const response = await apiClient.post('/interview/sessions', params);
  return response.data;
}
```

### 3.5 前端调用逻辑

```typescript
// 在 CompanyManagementPage.tsx 中

const handleStartInterview = async () => {
  try {
    // 1. 调用 API 创建面试会话
    const result = await startInterview({
      applicationId: selectedApplication.id,  // ⚠️ 必传：关联到岗位申请
      resumeId: selectedResume.id
    });

    // 2. 关闭弹窗
    setShowStartInterviewDialog(false);

    // 3. 跳转到面试页面
    navigate(`/interview/${result.sessionId}`);

  } catch (error) {
    toast.error('创建面试失败', {
      description: error.message
    });
  }
};
```

---

## 四、面试页面加载流程

```typescript
// 在 CompanyManagementPage.tsx 中

const handleStartInterview = async () => {
  try {
    // 1. 调用 API 创建面试会话
    const result = await startInterview({
      applicationId: selectedApplication.id,
      resumeId: selectedResume.id
    });

    // 2. 关闭弹窗
    setShowStartInterviewDialog(false);

    // 3. 跳转到面试页面
    navigate(`/interview/${result.sessionId}`);

  } catch (error) {
    toast.error('创建面试失败', {
      description: error.message
    });
  }
};
```

---

## 三、面试页面加载流程

### 4.1 页面初始化时序图

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐      ┌───────────────┐
│ InterviewPage│      │ interviewStore│      │ 后端 API     │      │ InterviewAgent │
└──────┬──────┘      └──────┬───────┘      └──────┬──────┘      └───────┬───────┘
       │                    │                     │                      │
       │  1. 组件挂载        │                     │                      │
       │                     │                     │                      │
       │  2. 从 URL 获取     │                     │                      │
       │     sessionId       │                     │                      │
       │ ────────────────────────────────────────────────────────────────>│
       │                    │                     │                      │
       │                    │  3. loadSession()   │                      │
       │                    │ ──────────────────>│                      │
       │                    │                     │                      │
       │                    │                     │  4. 查询 session     │
       │                    │                     │                      │
       │                    │  5. 返回 session信息 │                      │
       │                    │ <──────────────────│                      │
       │                    │                     │                      │
       │  6. 更新 session    │                     │                      │
       │     到 store        │                     │                      │
       │ <───────────────────│                     │                      │
       │                    │                     │                      │
       │  7. 加载历史消息     │                     │                      │
       │                    │ ──────────────────>│                      │
       │                    │                     │                      │
       │  8. 显示历史对话     │                     │                      │
       │ <───────────────────│                     │                      │
       │                    │                     │                      │
       │  9. 如果状态=WELCOME │                     │                      │
       │     显示欢迎消息     │                     │                      │
```

### 4.2 InterviewPage 组件结构

```tsx
// src/pages/InterviewPage.tsx

function InterviewPage() {
  const { sessionId } = useParams<{ sessionId: string }>();
  const {
    currentSession,
    messages,
    loading,
    sendMessage,
    loadSession,
    finishInterview
  } = useInterviewStore();

  // 页面加载时获取会话信息
  useEffect(() => {
    if (sessionId) {
      loadSession(sessionId);
    }
  }, [sessionId]);

  if (loading || !currentSession) {
    return <InterviewPageSkeleton />;
  }

  return (
    <div className="interview-page full-screen">
      {/* 顶部导航栏 */}
      <InterviewHeader
        company={currentSession.company}
        jobTitle={currentSession.jobTitle}
        sessionId={sessionId}
        onFinish={finishInterview}
      />

      <div className="interview-content">
        {/* 左侧：对话区域 */}
        <InterviewChat
          messages={messages}
          onSendMessage={sendMessage}
        />

        {/* 右侧：进度面板 */}
        <InterviewSidebar
          session={currentSession}
          stats={calculateStats(messages)}
        />
      </div>
    </div>
  );
}
```

---

## 五、面试对话流程

### 5.1 发送消息时序图

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐      ┌───────────────┐
│   用户输入   │      │ InterviewChat│      │ 后端 API     │      │ InterviewAgent │
└──────┬──────┘      └──────┬───────┘      └──────┬──────┘      └───────┬───────┘
       │                    │                     │                      │
       │  1. 输入消息并点击   │                     │                      │
       │     "发送"          │                     │                      │
       │ ──────────────────>│                     │                      │
       │                    │                     │                      │
       │  2. 立即显示用户消息 │                     │                      │
       │     (乐观更新)       │                     │                      │
       │ <───────────────────│                     │                      │
       │                    │                     │                      │
       │                    │  3. POST /api/interview/chat            │
       │                    │  {sessionId, message}                   │
       │                    │ ──────────────────────────────────────>  │
       │                    │                     │                      │
       │                    │                     │  4. 根据状态路由     │
       │                    │                     │ ───────────────────>│
       │                    │                     │                      │
       │                    │                     │  5. 三Agent协同决策 │
       │                    │                     │                      │
       │                    │                     │  6. 主面试官提问     │
       │                    │                     │ <───────────────────│
       │                    │                     │                      │
       │                    │  7. 返回 AI 响应    │                      │
       │                    │ <───────────────────────────────────────  │
       │                    │                     │                      │
       │  8. 追加 AI 回复    │                     │                      │
       │     到消息列表       │                     │                      │
       │ <───────────────────│                     │                      │
```

### 5.2 发送消息代码示例

```typescript
// src/store/interviewStore.ts

interface InterviewStore {
  // 发送面试消息
  sendMessage: (sessionId: string, content: string) => Promise<void>;
}

const sendMessage = async (sessionId: string, content: string) => {
  try {
    // 1. 立即添加用户消息到 UI
    const userMessage: InterviewMessage = {
      id: Date.now(),
      role: 'USER',
      content,
      timestamp: new Date().toISOString()
    };
    setMessages(prev => [...prev, userMessage]);

    // 2. 调用后端 API
    const response = await interviewApi.sendMessage({
      sessionId,
      message: content
    });

    // 3. 添加 AI 回复到 UI
    const aiMessage: InterviewMessage = {
      id: response.messageId,
      role: response.agentType, // MAIN_INTERVIEWER / VICE_INTERVIEWER / EVALUATOR
      content: response.content,
      timestamp: response.timestamp
    };
    setMessages(prev => [...prev, aiMessage]);

  } catch (error) {
    // 4. 发送失败，显示错误提示
    toast.error('发送消息失败');
  }
};
```

---

## 六、面试结束流程

### 6.1 结束面试时序图

```
┌─────────────┐      ┌──────────────┐      ┌─────────────┐      ┌───────────────┐
│   用户点击   │      │ InterviewPage│      │ 后端 API     │      │ InterviewAgent │
│  "结束面试"   │      └──────┬───────┘      └──────┬──────┘      └───────┬───────┘
└──────┬──────┘            │                     │                      │
       │                   │                     │                      │
       │  1. 确认对话框     │                     │                      │
       │ ─────────────────>│                     │                      │
       │                   │                     │                      │
       │  2. 用户确认结束   │                     │                      │
       │ ─────────────────>│                     │                      │
       │                   │                     │                      │
       │                   │  3. POST /api/interview/sessions/{id}/finish
       │                   │ ──────────────────────────────────────>    │
       │                   │                     │                      │
       │                   │                     │  4. 状态→FINISHED   │
       │                   │                     │                      │
       │                   │                     │  5. 评审专家生成报告 │
       │                   │                     │ ───────────────────>│
       │                   │                     │                      │
       │                   │                     │  6. 持久化所有记忆   │
       │                   │                     │                      │
       │                   │  7. 返回报告数据    │                      │
       │                   │ <───────────────────────────────────────   │
       │                   │                     │                      │
       │  8. 跳转到报告页面 │                     │                      │
       │ ─────────────────>│                     │                      │
       │                   │                     │                      │
       │                   │  9. navigate(`/interview/${id}/report`)
       │                   │                     │                      │
```

---

## 七、关键API接口汇总

### 7.1 创建面试会话

**⚠️ 重要说明**:
- `applicationId` 必传，关联到 JobApplication
- 后端会根据 applicationId 查询公司名和职位名

```typescript
POST /api/interview/sessions

Request:
{
  "applicationId": 123,
  "resumeId": 456
}

Response:
{
  "code": 200,
  "data": {
    "sessionId": "session-byt-001",
    "company": "字节跳动",
    "jobTitle": "Java后端工程师",
    "status": "IDLE",
    "createdAt": "2026-03-17T10:30:00"
  }
}
```

### 7.2 获取会话详情

```typescript
GET /api/interview/sessions/{sessionId}

Response:
{
  "code": 200,
  "data": {
    "sessionId": "session-byt-001",
    "company": "字节跳动",
    "jobTitle": "Java后端工程师",
    "status": "TECHNICAL_QA",
    "currentRound": 2,
    "totalRounds": 5,
    "createdAt": "2026-03-17T10:30:00"
  }
}
```

### 7.3 发送面试消息

```typescript
POST /api/interview/chat

Request:
{
  "sessionId": "session-byt-001",
  "message": "我认为Redis的缓存策略应该..."
}

Response:
{
  "code": 200,
  "data": {
    "messageId": "msg-789",
    "agentType": "MAIN_INTERVIEWER",
    "content": "很好，那你能详细讲讲Redis的...",
    "timestamp": "2026-03-17T10:35:00",
    "sessionState": "TECHNICAL_QA"
  }
}
```

### 7.4 获取聊天历史

```typescript
GET /api/interview/sessions/{sessionId}/messages

Response:
{
  "code": 200,
  "data": {
    "messages": [
      {
        "id": "msg-001",
        "role": "MAIN_INTERVIEWER",
        "content": "你好，欢迎来到字节跳动面试...",
        "timestamp": "2026-03-17T10:30:10"
      },
      {
        "id": "msg-002",
        "role": "USER",
        "content": "你好，我准备好了",
        "timestamp": "2026-03-17T10:30:30"
      }
    ]
  }
}
```

### 7.5 结束面试

```typescript
POST /api/interview/sessions/{sessionId}/finish

Response:
{
  "code": 200,
  "data": {
    "sessionId": "session-byt-001",
    "status": "FINISHED",
    "report": {
      "overallScore": 8.5,
      "credibilityScore": 0.85,
      "summary": "...",
      "improvements": ["..."]
    }
  }
}
```

---

## 八、路由配置

### 8.1 React Router 配置

```tsx
// src/App.tsx

<Routes>
  {/* 现有路由 */}
  <Route path="/" element={<WorkspacePage />} />
  <Route path="/companies" element={<CompanyManagementPage />} />

  {/* 模拟面试路由 - 独立页面 */}
  <Route path="/interviews" element={<InterviewListPage />} />
  <Route path="/interview/:sessionId" element={<InterviewPage />} />
  <Route path="/interview/:sessionId/report" element={<InterviewReportPage />} />
</Routes>
```

### 8.2 页面层级关系

```
┌─────────────────────────────────────────────────────────────┐
│  App Router                                                 │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  /interviews                                           │ │
│  │  └── InterviewListPage (会话列表页)                    │ │
│  │       └── 点击"继续面试" / "新建面试"                   │ │
│  └───────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  /interview/:sessionId                                │ │
│  │  └── InterviewPage (面试对话页 - 全屏)                 │ │
│  │       ├── InterviewHeader (顶部导航)                   │ │
│  │       ├── InterviewChat (对话区域)                     │ │
│  │       └── InterviewSidebar (进度侧边栏)                │ │
│  └───────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  /interview/:sessionId/report                         │ │
│  │  └── InterviewReportPage (面试报告页)                  │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 九、前端文件清单

### 9.1 新增文件

```
src/
├── pages/
│   ├── InterviewListPage.tsx              # 会话列表页面
│   ├── InterviewPage.tsx                  # 面试对话页面（全屏）
│   └── InterviewReportPage.tsx            # 面试报告页面
│
├── components/
│   └── interview/
│       ├── InterviewHeader.tsx            # 面试页面头部
│       ├── InterviewChat.tsx              # 面试对话组件
│       ├── InterviewSidebar.tsx           # 面试进度侧边栏
│       ├── InterviewStartDialog.tsx       # 面试启动弹窗
│       ├── SessionCard.tsx                # 会话卡片
│       └── report/
│           ├── ScoreCard.tsx              # 分数卡片
│           ├── ScoreDetails.tsx           # 评分明细
│           ├── CredibilityAnalysis.tsx    # 真实性分析
│           └── ImprovementSuggestions.tsx # 改进建议
│
├── services/
│   └── interviewApi.ts                    # 面试 API 服务
│
├── store/
│   └── interviewStore.ts                  # 面试状态管理
│
├── hooks/
│   ├── useSessionRestore.ts               # 会话恢复钩子
│   └── useLeaveConfirmation.ts            # 离开确认钩子
│
└── types/
    └── interview.ts                       # 面试类型定义
```

### 9.2 修改文件

```
src/
├── App.tsx                                # 添加面试相关路由
├── pages/
│   └── CompanyManagementPage.tsx          # 添加"模拟面试"按钮
│
└── components/
    └── layout/
        └── Header.tsx                     # 添加"面试"导航入口
```

---

## 十、状态流转图

```
┌─────────┐    ┌──────────┐    ┌──────────────┐    ┌──────────┐
│  IDLE   │ -> │ WELCOME  │ -> │ TECHNICAL_QA │ -> │ SCENARIO │
│ (初始)  │    │ (欢迎)   │    │  (技术问答)   │    │ (情景题) │
└─────────┘    └──────────┘    └──────────────┘    └──────────┘
                                                  │
                                                  v
                                          ┌──────────────┐    ┌───────────┐
                                          │ EVALUATION   │ -> │ FINISHED  │
                                          │  (综合评估)   │    │  (完成)   │
                                          └──────────────┘    └───────────┘
```

---

## 十一、常见问题

### Q1: 模拟面试和 JobApplication 是什么关系？

**A**: 一对多关系：
- 一个 `JobApplication` 可以有多个 `MockInterviewSession`
- 用户可以对同一个岗位申请重复进行模拟面试
- 每次面试都是独立的 `sessionId`，但都关联到同一个 `applicationId`

```
JobApplication (id=123, 字节跳动-Java后端)
    ├── MockInterviewSession (session-001, 第一次面试)
    ├── MockInterviewSession (session-002, 第二次面试)
    └── MockInterviewSession (session-003, 第三次面试)
```

### Q2: 如何从不同入口进入面试？

**A**: 有三种方式：
1. 从职位详情页点击"模拟面试"按钮
2. 从面试列表页点击"继续面试"
3. 从面试列表页点击"再次面试"创建新会话

### Q2: 面试页面和 AI 聊天助手有什么区别？

**A**: 完全独立：
- **路由不同**: 面试是 `/interview/:sessionId`，聊天是内嵌组件
- **状态隔离**: 使用不同的 Store (interviewStore vs chatStore)
- **API 不同**: 面试用 `/api/interview/*`，聊天用 `/api/chat/*`
- **Agent 不同**: 面试有3个协同 Agent，聊天是单个通用 Agent

### Q3: 如何处理页面刷新？

**A**: 使用 `useSessionRestore` hook：
- 从 URL 获取 `sessionId`
- 调用 `loadSession(sessionId)` 重新加载会话数据
- 加载历史消息并恢复状态

### Q4: 面试过程中如何防止意外离开？

**A**: 使用 `useLeaveConfirmation` hook：
- 监听 `beforeunload` 事件
- 显示浏览器原生确认对话框
- 仅在面试未完成时触发

---

**文档状态**: 已完成
