/**
 * BlockSuite 编辑器配置
 *
 * 注意：BlockSuite 的 API 已经改变
 * 当前版本使用 @blocksuite/store 和 @blocksuite/blocks
 * 后续可根据需要集成完整编辑器
 */

// 导出空对象，后续可以扩展
// BlockSuite 集成将在需要时添加
export const blockSuiteConfig = {
  version: '0.22.4',
  ready: false,
};

export default blockSuiteConfig;
