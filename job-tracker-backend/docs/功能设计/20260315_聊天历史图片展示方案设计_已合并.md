# 聊天历史图片展示方案设计

**创建日期**：2026-03-15
**作者**：Job Tracker Team
**状态**：✅ 已实现
**版本**：2.0（方案2：先上传图片，再关联消息）

---

## 📋 问题描述

### 背景

在多模态聊天功能中，图片采用**独立 chat_images 表 + 本地存储**的方案。

### 核心问题

1. **历史消息中的图片如何安全地展示？**
2. **图片上传流程如何优化？**（解决"鸡和蛋"问题：上传需要 messageId，但 messageId 需要先发送消息）

---

## 🎯 技术方案

### 方案概述

**方案2（已实现）**：先上传图片获得 imageId，再发送消息时关联

```
┌─────────────────────────────────────────────────────────────────┐
│                    新的上传和关联流程                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  【步骤1：上传图片】                                              │
│  1. 用户选择图片 → 点击上传按钮                                   │
│  2. HTTP POST /api/chat/upload/image?sessionKey=xxx             │
│  3. 后端保存文件 → 插入 chat_images (message_id=null)            │
│  4. 返回 { id: 123, publicUrl: "/api/chat/images/123?..." }    │
│                                                                 │
│  【步骤2：发送消息】                                              │
│  5. 用户输入文字（可选）                                         │
│  6. 点击发送 → WebSocket: { content, imageIds: [123], ... }     │
│  7. 后端保存消息 → 获得 messageId                                │
│  8. 后端更新 chat_images: SET message_id = xxx WHERE id IN (123)│
│                                                                 │
│  【步骤3：AI 处理】                                              │
│  9. 后端查询图片信息 → 转换为 Base64                             │
│  10. 调用 AI 模型                                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 安全访问流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    图片访问安全流程                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  【前端】                                                        │
│  1. 历史消息加载 → 获取图片信息                                 │
│     { id: 123, publicUrl: "/api/chat/images/123?sessionId=abc" }│
│  2. 渲染 <img src="/api/chat/images/123?sessionId=abc">         │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  【后端】                                                        │
│  3. 接收请求：GET /api/chat/images/{imageId}?sessionId=xxx       │
│  4. 查询数据库：SELECT * FROM chat_images WHERE id = {imageId}  │
│  5. 安全校验：图片所属会话 == 当前会话？                        │
│     - 校验通过 → 读取文件 → 返回图片                            │
│     - 校验失败 → 返回 403 Forbidden                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ 数据库设计

### 创建独立的图片表

**文件**：`src/main/resources/db/migration/V7__multimodal_support.sql`

```sql
-- 1. 创建独立的聊天图片表
CREATE TABLE chat_images (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '图片ID',
    message_id BIGINT NOT NULL COMMENT '关联的消息ID',
    session_id BIGINT NOT NULL COMMENT '所属会话ID（冗余字段，用于安全校验）',
    file_path VARCHAR(500) NOT NULL COMMENT '服务器内部相对路径',
    mime_type VARCHAR(100) NOT NULL COMMENT 'MIME 类型',
    file_name VARCHAR(255) COMMENT '原始文件名',
    file_size BIGINT COMMENT '文件大小（字节）',
    width INT COMMENT '图片宽度',
    height INT COMMENT '图片高度',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '上传时间',

    INDEX idx_message_id (message_id),
    INDEX idx_session_id (session_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='聊天图片表';

-- 2. chat_messages 表暂不添加 images 字段
-- 通过 message_id 关联查询 chat_images 表

-- 3. 添加索引优化
CREATE INDEX idx_chat_messages_session_created
ON chat_messages(session_id, created_at DESC);
```

### 表关系

```
chat_sessions (1) ───< (N) chat_messages (1) ───< (N) chat_images
                                           │
                                           └── 冗余的 session_id 用于安全校验
```

---

## 🔧 技术实现

### 1. 图片实体类

**文件**：`src/main/java/com/jobtracker/entity/ChatImage.java`

```java
package com.jobtracker.entity;

import com.baomidou.mybatisplus.annotation.*;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.io.Serializable;
import java.time.LocalDateTime;

/**
 * 聊天图片实体类
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@TableName("chat_images")
public class ChatImage implements Serializable {

    private static final long serialVersionUID = 1L;

    @TableId(value = "id", type = IdType.AUTO)
    private Long id;

    /**
     * 关联的消息ID
     */
    @TableField("message_id")
    private Long messageId;

    /**
     * 所属会话ID（冗余字段，用于安全校验）
     */
    @TableField("session_id")
    private Long sessionId;

    /**
     * 服务器内部相对路径
     */
    @TableField("file_path")
    private String filePath;

    /**
     * MIME 类型
     */
    @TableField("mime_type")
    private String mimeType;

    /**
     * 原始文件名
     */
    @TableField("file_name")
    private String fileName;

    /**
     * 文件大小（字节）
     */
    @TableField("file_size")
    private Long fileSize;

    /**
     * 图片宽度
     */
    @TableField("width")
    private Integer width;

    /**
     * 图片高度
     */
    @TableField("height")
    private Integer height;

    @TableField(value = "created_at", fill = FieldFill.INSERT)
    private LocalDateTime createdAt;
}
```

### 2. 图片 Mapper

**文件**：`src/main/java/com/jobtracker/mapper/ChatImageMapper.java`

```java
package com.jobtracker.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.jobtracker.entity.ChatImage;
import org.apache.ibatis.annotations.Mapper;

/**
 * 聊天图片 Mapper
 */
@Mapper
public interface ChatImageMapper extends BaseMapper<ChatImage> {
    // MyBatis Plus 提供基础 CRUD 方法
}
```

### 3. 图片附件 DTO

**文件**：`src/main/java/com/jobtracker/dto/ImageAttachment.java`

```java
package com.jobtracker.dto;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

/**
 * 图片附件（返回给前端的DTO）
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ImageAttachment {

    /**
     * 图片ID（用于访问接口）
     */
    private Long id;

    /**
     * 前端访问 URL
     * 格式：/api/chat/images/{id}?sessionId={sessionKey}
     */
    private String publicUrl;

    /**
     * MIME 类型
     */
    private String mimeType;

    /**
     * 原始文件名
     */
    private String fileName;

    /**
     * 文件大小（字节）
     */
    private Long fileSize;

    /**
     * 图片宽度
     */
    private Integer width;

    /**
     * 图片高度
     */
    private Integer height;

    /**
     * 从 ChatImage 实体创建 DTO
     */
    public static ImageAttachment fromEntity(ChatImage chatImage, String sessionKey) {
        return ImageAttachment.builder()
                .id(chatImage.getId())
                .publicUrl("/api/chat/images/" + chatImage.getId() + "?sessionId=" + sessionKey)
                .mimeType(chatImage.getMimeType())
                .fileName(chatImage.getFileName())
                .fileSize(chatImage.getFileSize())
                .width(chatImage.getWidth())
                .height(chatImage.getHeight())
                .build();
    }
}
```

### 4. 图片服务

**文件**：`src/main/java/com/jobtracker/service/ChatImageService.java`

```java
package com.jobtracker.service;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.core.conditions.update.LambdaUpdateWrapper;
import com.jobtracker.entity.ChatImage;
import com.jobtracker.mapper.ChatImageMapper;
import com.jobtracker.mapper.ChatSessionMapper;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;
import org.springframework.web.multipart.MultipartFile;

import java.io.IOException;
import java.util.List;

/**
 * 聊天图片服务
 */
@Slf4j
@Service
@RequiredArgsConstructor
public class ChatImageService {

    private final ChatImageMapper chatImageMapper;
    private final ChatSessionMapper chatSessionMapper;
    private final FileStorageService fileStorageService;

    /**
     * 保存图片（新流程：不依赖 messageId）
     *
     * @param file       上传的文件
     * @param sessionKey 会话标识（用于获取 sessionId）
     * @return 保存的图片实体
     */
    public ChatImage saveImage(MultipartFile file, String sessionKey) throws IOException {
        // 1. 查询会话获取 sessionId
        var session = chatSessionMapper.selectBySessionKey(sessionKey);
        if (session == null) {
            throw new IllegalArgumentException("会话不存在：sessionKey=" + sessionKey);
        }

        // 2. 校验文件类型
        String contentType = file.getContentType();
        if (contentType == null || !contentType.startsWith("image/")) {
            throw new IllegalArgumentException("只支持图片文件");
        }

        // 3. 保存文件到磁盘并获取相对路径
        String relativePath = fileStorageService.saveFileAndGetPath(file);

        // 4. 保存图片记录到数据库（messageId 暂时为 null）
        ChatImage chatImage = ChatImage.builder()
                .messageId(null)  // 暂时不关联消息
                .sessionId(session.getId())  // 冗余存储会话ID，用于安全校验
                .filePath(relativePath)
                .mimeType(contentType)
                .fileName(file.getOriginalFilename())
                .fileSize(file.getSize())
                .build();

        chatImageMapper.insert(chatImage);
        log.info("图片已保存：id={}, sessionKey={}, fileName={}",
                chatImage.getId(), sessionKey, file.getOriginalFilename());

        return chatImage;
    }

    /**
     * 更新图片关联的消息ID
     *
     * @param imageIds   图片ID列表
     * @param messageId 消息ID
     */
    public void updateMessageId(List<Long> imageIds, Long messageId) {
        if (imageIds == null || imageIds.isEmpty()) {
            return;
        }

        // 使用 LambdaUpdateWrapper 批量更新
        chatImageMapper.update(null,
                new LambdaUpdateWrapper<ChatImage>()
                        .in(ChatImage::getId, imageIds)
                        .set(ChatImage::getMessageId, messageId)
        );

        log.info("已更新图片关联：messageId={}, imageIds={}, count={}",
                messageId, imageIds, imageIds.size());
    }

    /**
     * 根据 ID 和 sessionKey 获取图片（带安全校验）
     *
     * @param imageId    图片ID
     * @param sessionKey 会话标识（前端传入）
     * @return 图片实体
     * @throws IllegalArgumentException 校验失败
     */
    public ChatImage getImageWithAuth(Long imageId, String sessionKey) {
        // 1. 查询图片
        ChatImage chatImage = chatImageMapper.selectById(imageId);
        if (chatImage == null) {
            throw new IllegalArgumentException("图片不存在");
        }

        // 2. 查询会话
        var session = chatSessionMapper.selectBySessionKey(sessionKey);
        if (session == null) {
            throw new IllegalArgumentException("会话不存在");
        }

        // 3. 安全校验：图片所属会话 == 当前会话
        if (!chatImage.getSessionId().equals(session.getId())) {
            log.warn("图片访问被拒绝：imageId={}, imageSessionId={}, requestSessionKey={}",
                    imageId, chatImage.getSessionId(), sessionKey);
            throw new SecurityException("无权访问该图片");
        }

        return chatImage;
    }

    /**
     * 获取消息关联的所有图片
     *
     * @param messageId 消息ID
     * @return 图片列表
     */
    public List<ChatImage> getImagesByMessageId(Long messageId) {
        return chatImageMapper.selectList(
                new LambdaQueryWrapper<ChatImage>()
                        .eq(ChatImage::getMessageId, messageId)
                        .orderByAsc(ChatImage::getCreatedAt)
        );
    }

    /**
     * 根据 ID 获取图片（无校验）
     *
     * @param imageId 图片ID
     * @return 图片实体，不存在则返回 null
     */
    public ChatImage getById(Long imageId) {
        return chatImageMapper.selectById(imageId);
    }
}
```

### 5. 图片访问控制器

**文件**：`src/main/java/com/jobtracker/controller/ChatController.java`

```java
package com.jobtracker.controller;

import com.jobtracker.common.result.Result;
import com.jobtracker.entity.ChatImage;
import com.jobtracker.service.ChatImageService;
import com.jobtracker.service.FileStorageService;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.http.CacheControl;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import java.io.IOException;
import java.util.concurrent.TimeUnit;

/**
 * 聊天控制器
 */
@Slf4j
@RestController
@RequestMapping("/chat")
@RequiredArgsConstructor
public class ChatController {

    private final ChatImageService chatImageService;
    private final FileStorageService fileStorageService;

    /**
     * 获取图片（带安全校验）
     * <p>
     * 访问格式：GET /api/chat/images/{imageId}?sessionId={sessionKey}
     * </p>
     *
     * @param imageId    图片ID
     * @param sessionId 会话标识（用于安全校验）
     * @return 图片文件
     */
    @GetMapping("/images/{imageId}")
    public ResponseEntity<byte[]> getImage(
            @PathVariable Long imageId,
            @RequestParam String sessionId) {

        try {
            // 1. 带安全校验获取图片信息
            ChatImage chatImage = chatImageService.getImageWithAuth(imageId, sessionId);

            // 2. 读取文件
            byte[] imageBytes = fileStorageService.readFileAsBytes(chatImage.getFilePath());

            // 3. 返回图片（带缓存）
            return ResponseEntity.ok()
                    .contentType(org.springframework.http.MediaType.parseMediaType(chatImage.getMimeType()))
                    .cacheControl(CacheControl.maxAge(7, TimeUnit.DAYS))
                    .body(imageBytes);

        } catch (IllegalArgumentException e) {
            log.warn("图片不存在：imageId={}, sessionId={}", imageId, sessionId);
            return ResponseEntity.notFound().build();

        } catch (SecurityException e) {
            log.warn("图片访问被拒绝：imageId={}, sessionId={}", imageId, sessionId);
            return ResponseEntity.status(403).build();

        } catch (IOException e) {
            log.error("读取图片失败：imageId={}, sessionId={}", imageId, sessionId, e);
            return ResponseEntity.internalServerError().build();
        }
    }

    /**
     * 上传图片（新流程：使用 sessionKey）
     *
     * @param file       上传的文件
     * @param sessionKey 会话标识（用于安全校验）
     * @return 图片信息（包含 imageId 和 publicUrl）
     */
    @PostMapping("/upload/image")
    public Result<ChatImage> uploadImage(
            @RequestParam("file") MultipartFile file,
            @RequestParam("sessionKey") String sessionKey) {
        try {
            ChatImage chatImage = chatImageService.saveImage(file, sessionKey);
            return Result.success("上传成功", chatImage);
        } catch (IOException e) {
            log.error("文件上传失败：sessionKey={}", sessionKey, e);
            return Result.error("文件上传失败：" + e.getMessage());
        } catch (IllegalArgumentException e) {
            log.warn("上传失败：sessionKey={}", sessionKey, e);
            return Result.error(e.getMessage());
        }
    }
}
```

### 6. 修改 ChatHistoryService

**文件**：`src/main/java/com/jobtracker/service/ChatHistoryService.java`

```java
/**
 * 保存用户消息（支持图片附件）
 *
 * @param sessionKey 会话标识
 * @param content    消息内容
 * @return 保存的消息对象
 */
@Transactional(rollbackFor = Exception.class)
public ChatMessage saveUserMessage(String sessionKey, String content) {
    ChatSession session = getOrCreateSession(sessionKey);

    ChatMessage message = ChatMessage.builder()
            .sessionId(session.getId())
            .role("USER")
            .content(content)
            .build();
    messageMapper.insert(message);

    sessionMapper.incrementMessageCount(session.getId());
    log.debug("保存用户消息：sessionId={}, messageId={}", session.getId(), message.getId());

    return message;
}

/**
 * 保存用户消息的图片附件
 *
 * @param messageId 消息ID
 * @param files     图片文件列表
 * @return 保存的图片列表
 */
@Transactional(rollbackFor = Exception.class)
public List<ChatImage> saveMessageImages(Long messageId, List<MultipartFile> files) {
    List<ChatImage> images = new ArrayList<>();

    for (MultipartFile file : files) {
        try {
            ChatImage chatImage = chatImageService.saveImage(file, messageId);
            images.add(chatImage);
        } catch (IOException e) {
            log.error("保存图片失败：messageId={}", messageId, e);
        }
    }

    return images;
}

/**
 * 获取会话历史消息（含图片信息）
 *
 * @param sessionKey 会话标识
 * @return 消息列表（含图片）
 */
public List<ChatMessageWithImages> getSessionMessagesWithImages(String sessionKey) {
    ChatSession session = sessionMapper.selectBySessionKey(sessionKey);
    if (session == null) {
        return List.of();
    }

    // 查询消息列表
    List<ChatMessage> messages = messageMapper.selectBySessionId(session.getId());

    // 转换为带图片的消息 DTO
    List<ChatMessageWithImages> result = new ArrayList<>();
    for (ChatMessage message : messages) {
        // 查询消息关联的图片
        List<ChatImage> images = chatImageService.getImagesByMessageId(message.getId());

        // 转换为前端需要的格式
        List<ImageAttachment> imageAttachments = images.stream()
                .map(img -> ImageAttachment.fromEntity(img, sessionKey))
                .toList();

        result.add(ChatMessageWithImages.builder()
                .id(message.getId())
                .role(message.getRole())
                .content(message.getContent())
                .images(imageAttachments)
                .createdAt(message.getCreatedAt())
                .build());
    }

    return result;
}
```

### 7. WebSocket 消息处理（含图片）

**文件**：`src/main/java/com/jobtracker/dto/WebSocketMessage.java`

```java
/**
 * WebSocket 消息 DTO
 */
@Data
@Builder
public class WebSocketMessage {

    private String type;
    private String content;
    private String sessionId;
    private Long timestamp;

    /**
     * 图片ID列表（多模态消息）
     * 用户先上传图片获得 imageId，发送消息时携带 imageId 列表
     */
    private List<Long> imageIds;

    // ... 其他方法和字段 ...
}
```

**文件**：`src/main/java/com/jobtracker/websocket/ChatWebSocketHandler.java`

```java
@Override
protected void handleTextMessage(WebSocketSession session, TextMessage message) throws Exception {
    try {
        WebSocketMessage wsMessage = objectMapper.readValue(payload, WebSocketMessage.class);
        String messageType = wsMessage.getType();
        String content = wsMessage.getContent();
        List<Long> imageIds = wsMessage.getImageIds();

        // 验证：内容和图片不能同时为空
        boolean hasContent = content != null && !content.trim().isEmpty();
        boolean hasImages = imageIds != null && !imageIds.isEmpty();

        if (!hasContent && !hasImages) {
            // 返回错误...
            return;
        }

        String chatSessionKey = wsMessage.getSessionId();

        // 1. 保存用户消息（传入 imageIds 用于关联）
        ChatMessage savedMessage = chatHistoryService.saveUserMessage(
            chatSessionKey, content, imageIds);

        // 2. 查询图片信息（用于 AI 调用）
        List<ImageAttachment> images = null;
        if (hasImages) {
            images = imageIds.stream()
                    .map(chatImageService::getById)
                    .filter(img -> img != null)
                    .map(img -> ImageAttachment.fromEntity(img, chatSessionKey))
                    .toList();
        }

        // 3. 调用 AI Agent
        JobAgent jobAgent = jobAgentFactory.createAgent(chatSessionKey);
        String aiResponse;

        if (hasImages && images != null && !images.isEmpty()) {
            aiResponse = jobAgent.chatWithImages(content, images, currentDate, currentTime, dayOfWeek);
        } else {
            aiResponse = jobAgent.chat(content, currentDate, currentTime, dayOfWeek);
        }

        // 4. 保存 AI 响应
        chatHistoryService.saveAssistantMessage(chatSessionKey, aiResponse);

        // 5. 发送响应
        WebSocketMessage responseMessage = WebSocketMessage.chat(aiResponse);
        session.sendMessage(new TextMessage(objectMapper.writeValueAsString(responseMessage)));

    } catch (Exception e) {
        log.error("处理消息失败", e);
    }
}
```

---

## 🌐 前端实现

### 1. 类型定义

**文件**：`frontend/src/types/chat.ts`

```typescript
/**
 * 图片附件
 */
export interface ImageAttachment {
  id: number;           // 图片ID
  publicUrl: string;    // 访问URL：/api/chat/images/{id}?sessionId={sessionKey}
  mimeType: string;
  fileName?: string;
  fileSize?: number;
  width?: number;
  height?: number;
}

/**
 * 聊天消息
 */
export interface ChatMessage {
  id?: number;
  role: 'user' | 'assistant';
  content: string;
  timestamp: number;
  images?: ImageAttachment[];
}
```

### 2. 图片上传流程（新）

**文件**：`frontend/src/components/chat/ImageUploader.tsx`

```typescript
const handleFileSelect = async (e: React.ChangeEvent<HTMLInputElement>) => {
  const files = Array.from(e.target.files || []);

  for (const file of files) {
    // 验证文件类型和大小...

    // 1. 先上传图片（只需 sessionKey，不需要 messageId）
    const formData = new FormData();
    formData.append('file', file);
    formData.append('sessionKey', currentSessionKey);  // 使用 sessionKey

    const response = await chatApi.uploadImage(formData);

    // 2. 保存 imageId 和 publicUrl
    const attachment: ImageAttachment = {
      id: response.data.id,
      publicUrl: `/api/chat/images/${response.data.id}?sessionId=${currentSessionKey}`,
      mimeType: response.data.mimeType,
      fileName: file.name,
      fileSize: file.size
    };

    onImagesChange([...images, attachment]);
  }
};

// 发送消息时
const handleSend = async () => {
  // 收集所有图片的 ID
  const imageIds = images.map(img => img.id);

  // WebSocket 发送：只传 ID，不传完整对象
  webSocketManager.send({
    type: 'CHAT',
    content: input,
    sessionId: currentSessionKey,
    imageIds  // 新字段：只传 ID 列表
  });

  // 清空输入和图片
  setInput('');
  setImages([]);
};
```

### 2.1 API 服务

**文件**：`frontend/src/services/chatApi.ts`

```typescript
export const chatApi = {
  /**
   * 上传图片（新接口：使用 sessionKey）
   */
  uploadImage: async (file: File, sessionKey: string) => {
    const formData = new FormData();
    formData.append('file', file);
    formData.append('sessionKey', sessionKey);

    const response = await axios.post('/chat/upload/image', formData, {
      headers: { 'Content-Type': 'multipart/form-data' }
    });

    return response.data.data;  // { id, publicUrl, mimeType, ... }
  },

  /**
   * 获取会话历史（含图片）
   */
  getSessionMessagesWithImages: async (sessionKey: string) => {
    const response = await axios.get(`/chat/sessions/${sessionKey}/messages-with-images`);
    return response.data.data;
  }
};
```

### 3. 图片展示组件

**文件**：`frontend/src/components/chat/ChatMessageImage.tsx`

```typescript
interface ChatMessageImageProps {
  image: ImageAttachment;
  alt?: string;
}

export const ChatMessageImage: React.FC<ChatMessageImageProps> = ({ image, alt }) => {
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(false);

  return (
    <div className="inline-block">
      {error ? (
        // 占位图
        <div className="flex items-center gap-2 p-4 bg-gray-100 rounded-lg">
          <ImageOff className="w-8 h-8 text-gray-400" />
          <span className="text-sm text-gray-500">图片无法加载</span>
        </div>
      ) : (
        <img
          src={image.publicUrl}
          alt={alt || image.fileName || '图片'}
          className={`max-w-full h-auto rounded-lg ${
            loading ? 'opacity-0' : 'opacity-100'
          }`}
          onLoad={() => setLoading(false)}
          onError={() => {
            setLoading(false);
            setError(true);
          }}
        />
      )}
    </div>
  );
};
```

---

## ✅ 安全性分析

### 攻击场景防护

| 攻击类型 | 原始风险 | 新方案防护 |
|---------|---------|-----------|
| **路径遍历** | `/api/chat/images/../../private.jpg` | ✅ 通过 ID 访问，无法遍历路径 |
| **会话越权** | 修改 URL 访问其他会话图片 | ✅ 校验 `sessionId` 是否匹配 |
| **暴力枚举** | 枚举 ID 获取所有图片 | ✅ 需要知道正确的 `sessionId` |
| **时间攻击** | 修改时间戳绕过 | ✅ 不依赖时间戳 |

### 校验流程

```
1. 前端请求：GET /api/chat/images/123?sessionId=abc
                  ↓
2. 后端查询：SELECT * FROM chat_images WHERE id = 123
                  ↓
3. 校验会话：SELECT * FROM chat_sessions WHERE session_key = 'abc'
                  ↓
4. 比对 sessionId：
   - chat_image.session_id == chat_session.id ?
     - ✅ 通过 → 返回图片
     - ❌ 失败 → 返回 403 Forbidden
```

---

## 📊 数据流图

```
┌─────────────────────────────────────────────────────────────────┐
│                    完整的多模态消息流程（方案2）                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  【步骤1：上传图片】                                              │
│  1. 用户选择图片 → 点击上传按钮                                   │
│  2. HTTP POST /api/chat/upload/image                            │
│     参数：file (图片文件), sessionKey (会话标识)                  │
│  3. 后端处理：                                                   │
│     - 保存文件到磁盘                                             │
│     - 插入 chat_images 表 (message_id=null, session_id=xxx)      │
│     - 返回 { id: 123, publicUrl: "/api/chat/images/123?..." }  │
│  4. 前端保存 imageId=123，预览图片                               │
│                                                                 │
│  【步骤2：发送消息】                                              │
│  5. 用户输入文字（可选）                                         │
│  6. 点击发送按钮                                                 │
│  7. WebSocket 发送：                                             │
│     {                                                           │
│       type: "CHAT",                                            │
│       content: "帮我看看这份简历",                               │
│       sessionId: "abc",                                        │
│       imageIds: [123, 124]  // 注意：只传 ID，不传完整对象       │
│     }                                                          │
│                                                                 │
│  【步骤3：后端处理】                                              │
│  8. 保存消息到 chat_messages → 获得 messageId=456                │
│  9. 批量更新 chat_images：                                       │
│     UPDATE chat_images SET message_id=456                       │
│     WHERE id IN (123, 124)                                      │
│  10. 查询图片信息 → 转换为 Base64 → 调用 AI                      │
│  11. 保存 AI 响应 → 返回给前端                                  │
│                                                                 │
│  【步骤4：历史消息加载】                                          │
│  1. 前端请求：GET /api/chat/sessions/{sessionKey}/messages-with-images
│  2. 后端查询消息 + 关联图片（通过 message_id JOIN）              │
│  3. 返回格式：                                                   │
│     [{                                                          │
│       id: 456,                                                 │
│       role: "USER",                                            │
│       content: "帮我看看这份简历",                              │
│       images: [{                                                │
│         id: 123,                                                │
│         publicUrl: "/api/chat/images/123?sessionId=abc"        │
│       }]                                                        │
│     }]                                                          │
│  4. 前端渲染：<img src="/api/chat/images/123?sessionId=abc">   │
│  5. 浏览器请求图片 → 后端安全校验 → 返回图片                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 关键变化

| 项目 | 旧方案（有问题） | 新方案（已实现） |
|------|----------------|----------------|
| **上传时机** | 需要先有 messageId | 可以先上传（只需 sessionKey） |
| **上传参数** | file + messageId | file + sessionKey |
| **message_id** | 上传时就设置 | 先为 null，发送消息后更新 |
| **WebSocket** | 传完整的 ImageAttachment[] | 只传 imageIds: Long[] |
| **流程顺序** | 发消息 → 上传图片 | 上传图片 → 发消息 |

---

## 📝 实施步骤

### 阶段 1：数据库和实体（1天）✅ 已完成
1. ✅ 执行数据库迁移 V7
2. ✅ 创建 ChatImage 实体
3. ✅ 创建 ChatImageMapper
4. ✅ 创建 ImageAttachment DTO

### 阶段 2：服务和控制器（1-2天）✅ 已完成
1. ✅ 创建 ChatImageService（支持 sessionKey 上传）
2. ✅ 实现 ChatController.getImage()（带安全校验）
3. ✅ 实现 ChatController.uploadImage()（使用 sessionKey）
4. ✅ 修改 ChatHistoryService.saveUserMessage()（支持 imageIds）
5. ✅ 修改 ChatWebSocketHandler（处理 imageIds）

### 阶段 3：前端开发（1-2天）⏳ 待开发
1. ⏳ 修改 ImageUploader（使用 sessionKey 上传）
2. ⏳ 创建 ChatMessageImage 组件
3. ⏳ 修改 chatApi.uploadImage()（传 sessionKey）
4. ⏳ 修改 WebSocket 消息格式（imageIds 替代 images）
5. ⏳ 修改 ChatStore

### 阶段 4：测试（0.5天）⏳ 待测试
1. ⏳ 正常流程测试
2. ⏳ 安全性测试（越权访问）
3. ⏳ 边界情况测试（无文字只发图片）

---

## 🔄 方案变更记录

### v2.0（当前实现） - 2026-03-15

**变更原因**：原方案（v1.0）存在"鸡和蛋"问题 - 上传图片需要 messageId，但 messageId 需要先发送消息才能获得。

**变更内容**：

| 项目 | v1.0（废弃） | v2.0（当前） |
|------|-------------|-------------|
| 上传参数 | `messageId`（必需） | `sessionKey`（必需） |
| message_id | 上传时设置 | 先 null，发送消息后更新 |
| WebSocket | `images: ImageAttachment[]` | `imageIds: Long[]` |
| 流程顺序 | 发消息 → 上传图片 | 上传图片 → 发消息 |

**新增方法**：
- `ChatImageService.updateMessageId(List<Long> imageIds, Long messageId)` - 批量关联图片到消息

**删除方法**：
- `ChatHistoryService.saveMessageImages()` - 不再需要

---

## ✅ 验收标准

### 功能验收
- [x] 后端可以上传图片（使用 sessionKey）
- [x] 上传后返回 imageId 和 publicUrl
- [x] WebSocket 可以接收 imageIds 列表
- [x] 保存消息后自动关联图片
- [ ] 前端可以选择并上传图片
- [ ] 支持图片预览和删除
- [ ] 支持图片 + 文字混合输入
- [ ] AI 能够分析图片内容
- [ ] 聊天历史能够显示图片

### 安全验收
- [x] 图片访问需要 sessionId 校验
- [x] 跨会话访问返回 403
- [x] 图片 ID 无法被枚举攻击

### 性能验收
- [ ] 图片上传延迟 < 3 秒
- [ ] AI 响应延迟 < 10 秒（含图片处理）

---

## 📎 相关文档

- ~~[多模态聊天图片文字输入功能设计](多模态聊天图片文字输入功能设计_20260315.md)~~ （已废弃）
- [聊天历史加载设计](聊天历史加载设计_20260315.md)
