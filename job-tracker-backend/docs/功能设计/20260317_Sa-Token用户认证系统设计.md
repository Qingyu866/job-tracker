# Sa-Token 用户认证系统设计

**创建日期**: 2026-03-17
**密级**: 内部公开
**状态**: 设计中

---

## 一、设计概述

### 1.1 问题背景

当前系统中大量接口依赖 `userId` 参数：

| 模块 | 接口 | userId 用途 |
|------|------|-------------|
| 模拟面试 | `POST /mock-interview/start` | 创建面试会话时关联用户 |
| 模拟面试 | `GET /mock-interview/sessions/user/{userId}` | 获取用户所有面试 |
| 简历管理 | `GET /resumes/user/{userId}` | 获取用户简历列表 |
| 简历管理 | `PUT /resumes/{id}/default` | 设置默认简历 |
| OCR 识别 | `OcrCallRecord.userId` | 记录 OCR 调用归属 |

**问题**：
- 无登录功能，任何用户都可以访问其他用户的数据
- 前端需要手动传递 userId，安全性差
- 无法实现权限控制和使用配额管理

### 1.2 解决方案

引入 **Sa-Token** 轻量级权限认证框架：

```
Sa-Token
├── 登录认证：用户名密码登录
├── Token 管理：自动生成、刷新、失效
├── 会话管理：单端登录、多端登录
├── 权限认证：角色权限、接口权限
└── 持久化：Redis 存储 Token
```

### 1.3 核心目标

1. **安全性**: userId 从 Token 中获取，前端无法伪造
2. **便捷性**: 前端只需在 Header 中携带 Token
3. **扩展性**: 支持多端登录、踢人下线、记住我等功能
4. **兼容性**: 最小化对现有接口的改动

---

## 二、Sa-Token 框架介绍

### 2.1 什么是 Sa-Token？

**Sa-Token** 是一个轻量级 Java 权限认证框架，主要解决：**登录认证、权限认证、单点登录、OAuth2.0** 等问题。

**官网**: https://sa-token.cc/

**核心特性**：
- 简单：只需 3 行代码即可实现登录
- 强大：支持单端登录、多端登录、踢人下线
- 扩展：权限认证、角色认证、Session 会话管理
- 稳定：经过多年生产验证

### 2.2 对比其他框架

| 框架 | 优点 | 缺点 |
|------|------|------|
| **Spring Security** | 功能强大、生态完善 | 配置复杂、学习曲线陡峭 |
| **Apache Shiro** | 灵活、简单 | 社区活跃度下降 |
| **Sa-Token** | 简单易用、文档完善、中文社区 | 相对新、生态较小 |
| **JWT** | 无状态、跨语言 | 无法失效、需要额外实现 |

**选择 Sa-Token 的理由**：
- 项目是单体应用，不需要复杂的分布式认证
- 需要快速实现，Sa-Token 配置简单
- 中文文档完善，问题易解决

---

## 三、数据库设计

### 3.1 用户表 (sys_user)

```sql
CREATE TABLE `sys_user` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '用户ID',
    `username` VARCHAR(50) NOT NULL COMMENT '用户名（登录名）',
    `password` VARCHAR(128) NOT NULL COMMENT '密码（BCrypt加密）',
    `nickname` VARCHAR(50) DEFAULT NULL COMMENT '昵称',
    `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
    `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
    `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-禁用, 1-正常',
    `last_login_time` DATETIME DEFAULT NULL COMMENT '最后登录时间',
    `last_login_ip` VARCHAR(50) DEFAULT NULL COMMENT '最后登录IP',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除, 1-已删除',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`),
    UNIQUE KEY `uk_email` (`email`),
    UNIQUE KEY `uk_phone` (`phone`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统用户表';
```

### 3.2 用户角色表 (sys_user_role)

```sql
CREATE TABLE `sys_user_role` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
    `role_code` VARCHAR(50) NOT NULL COMMENT '角色代码：USER/ADMIN',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_role` (`user_id`, `role_code`),
    KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联表';
```

### 3.3 初始数据

```sql
-- 默认管理员账户：admin / 123456
INSERT INTO `sys_user` (`username`, `password`, `nickname`, `status`)
VALUES (
    'admin',
    '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iAt6Z5EH',
    '系统管理员',
    1
);

-- 管理员角色
INSERT INTO `sys_user_role` (`user_id`, `role_code`)
VALUES (1, 'ADMIN');
```

---

## 四、后端实现

### 4.1 Maven 依赖

```xml
<!-- pom.xml -->
<dependency>
    <groupId>cn.dev33</groupId>
    <artifactId>sa-token-spring-boot3-starter</artifactId>
    <version>1.37.0</version>
</dependency>
<dependency>
    <groupId>cn.dev33</groupId>
    <artifactId>sa-token-redis-jackson</artifactId>
    <version>1.37.0</version>
</dependency>
```

### 4.2 配置文件

```yaml
# application.yml

sa-token:
  # Token 名称（也是 Cookie 名称）
  token-name: satoken
  # Token 有效期（秒）- 7天
  timeout: 604800
  # 是否允许同一账号多地同时登录
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 Token
  is-share: false
  # Token 风格（可选值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: uuid
  # 是否输出操作日志
  is-log: false
  # 是否从 Cookie 读取 Token
  is-read-cookie: false
  # 是否从 Header 读取 Token
  is-read-header: true
  # Token Header 名称
  token-prefix: Bearer
```

### 4.3 核心类结构

```
src/main/java/com/jobtracker/
├── auth/
│   ├── AuthController.java               # 认证控制器
│   ├── AuthService.java                   # 认证服务
│   ├── AuthConfig.java                    # Sa-Token 配置
│   ├── context/
│   │   ├── UserContext.java               # 用户上下文（ThreadLocal）
│   │   └── UserInfoHolder.java            # 用户信息持有者
│   ├── interceptor/
│   │   ├── LoginInterceptor.java          # 登录拦截器
│   │   └── AdminInterceptor.java          # 管理员拦截器
│   ├── exception/
│   │   └── NotLoginException.java         # 未登录异常处理
│   └── annotation/
│       └── RequireLogin.java              # 自定义登录注解
│
├── entity/
│   └── SysUser.java                       # 用户实体
│
├── mapper/
│   └── SysUserMapper.java                 # 用户 Mapper
│
└── utils/
    └── PasswordUtil.java                  # 密码加密工具（使用 Sa-Token）
```

### 4.3.1 用户上下文类

**核心类**: `UserContext` - 使用 ThreadLocal 存储当前请求的用户信息

```java
/**
 * 用户上下文 - 存储当前请求的用户信息
 * 类似 Spring Security 的 SecurityContextHolder
 */
public class UserContext {

    private static final ThreadLocal<UserInfoHolder> CONTEXT_HOLDER = new ThreadLocal<>();

    /**
     * 设置当前用户信息
     */
    public static void setCurrentUser(UserInfoHolder userInfo) {
        CONTEXT_HOLDER.set(userInfo);
    }

    /**
     * 获取当前用户信息
     */
    public static UserInfoHolder getCurrentUser() {
        UserInfoHolder holder = CONTEXT_HOLDER.get();
        if (holder == null) {
            // 从 Sa-Token 获取并设置
            Long userId = StpUtil.getLoginIdAsLong();
            holder = UserInfoHolder.builder()
                .userId(userId)
                .username(StpUtil.getSession().getString("username"))
                .build();
            CONTEXT_HOLDER.set(holder);
        }
        return holder;
    }

    /**
     * 获取当前用户 ID
     */
    public static Long getCurrentUserId() {
        UserInfoHolder holder = getCurrentUser();
        if (holder == null) {
            throw new NotLoginException("未登录或登录已过期");
        }
        return holder.getUserId();
    }

    /**
     * 判断是否已登录
     */
    public static boolean isLoggedIn() {
        return StpUtil.isLogin();
    }

    /**
     * 清除当前用户信息（请求结束时调用）
     */
    public static void clear() {
        CONTEXT_HOLDER.remove();
    }
}
```

```java
/**
 * 用户信息持有者
 */
@Data
@Builder
public class UserInfoHolder implements Serializable {
    private static final long serialVersionUID = 1L;

    /**
     * 用户 ID
     */
    private Long userId;

    /**
     * 用户名
     */
    private String username;

    /**
     * 昵称
     */
    private String nickname;

    /**
     * 头像
     */
    private String avatar;

    /**
     * 邮箱
     */
    private String email;

    /**
     * 手机号
     */
    private String phone;

    /**
     * 角色列表
     */
    private Set<String> roles;

    /**
     * 权限列表
     */
    private Set<String> permissions;
}
```

### 4.3.2 拦截器 - 自动设置用户上下文

```java
/**
 * Sa-Token 登录拦截器
 * 在请求开始时设置用户上下文，请求结束时清理
 */
@Component
public class LoginInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        // 如果已登录，设置用户上下文
        if (StpUtil.isLogin()) {
            Long userId = StpUtil.getLoginIdAsLong();
            String username = StpUtil.getSession().getString("username");

            UserInfoHolder holder = UserInfoHolder.builder()
                .userId(userId)
                .username(username)
                .build();

            UserContext.setCurrentUser(holder);
        }

        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response,
                                Object handler, Exception ex) {
        // 请求结束时清理 ThreadLocal
        UserContext.clear();
    }
}
```

### 4.4 用户实体类

```java
@Data
@TableName("sys_user")
public class SysUser implements Serializable {
    private static final long serialVersionUID = 1L;

    @TableId(value = "id", type = IdType.AUTO)
    private Long id;

    private String username;
    private String password;
    private String nickname;
    private String avatar;
    private String email;
    private String phone;
    private Integer status;
    private LocalDateTime lastLoginTime;
    private String lastLoginIp;

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updatedAt;

    @TableLogic
    private Integer deleted;
}
```

### 4.5 认证控制器

```java
@RestController
@RequestMapping("/auth")
@RequiredArgsConstructor
public class AuthController {

    private final AuthService authService;

    /**
     * 用户登录
     * POST /api/auth/login
     */
    @PostMapping("/login")
    public ApiResponse<LoginResponse> login(@RequestBody LoginRequest request) {
        LoginResponse response = authService.login(request);
        return ApiResponse.success("登录成功", response);
    }

    /**
     * 用户注册
     * POST /api/auth/register
     */
    @PostMapping("/register")
    public ApiResponse<Void> register(@RequestBody RegisterRequest request) {
        authService.register(request);
        return ApiResponse.success("注册成功");
    }

    /**
     * 获取当前用户信息
     * GET /api/auth/info
     */
    @GetMapping("/info")
    public ApiResponse<UserInfo> getCurrentUser() {
        UserInfo userInfo = authService.getCurrentUserInfo();
        return ApiResponse.success(userInfo);
    }

    /**
     * 退出登录
     * POST /api/auth/logout
     */
    @PostMapping("/logout")
    public ApiResponse<Void> logout() {
        StpUtil.logout();
        return ApiResponse.success("退出成功");
    }

    /**
     * 修改密码
     * POST /api/auth/change-password
     */
    @PostMapping("/change-password")
    public ApiResponse<Void> changePassword(@RequestBody ChangePasswordRequest request) {
        authService.changePassword(request);
        return ApiResponse.success("密码修改成功");
    }
}
```

### 4.6 密码加密工具（使用 Sa-Token）

```java
/**
 * 密码加密工具类 - 使用 Sa-Token 的 SaSecureUtil
 *
 * 加密策略：SHA256 + 盐
 * - 盐值：用户名 + 固定盐
 * - 散列次数：1024 次
 */
public class PasswordUtil {

    /**
     * 固定盐值（生产环境应配置在配置文件中）
     */
    private static final String SALT = "job_tracker_sa_token_salt_2026";

    /**
     * 散列次数
     */
    private static final int HASH_ITERATIONS = 1024;

    /**
     * 加密密码
     *
     * @param password 明文密码
     * @param username 用户名（作为盐的一部分）
     * @return 加密后的密码
     */
    public static String encrypt(String password, String username) {
        // 1. 生成组合盐：用户名 + 固定盐
        String combinedSalt = username + SALT;

        // 2. 第一次 SHA256 加密
        String hash1 = SaSecureUtil.sha256(password + combinedSalt);

        // 3. 多次散列增强安全性
        String hash = hash1;
        for (int i = 0; i < HASH_ITERATIONS; i++) {
            hash = SaSecureUtil.sha256(hash + combinedSalt);
        }

        return hash;
    }

    /**
     * 验证密码
     *
     * @param password 明文密码
     * @param username 用户名
     * @param hashedPassword 数据库中存储的加密密码
     * @return 是否匹配
     */
    public static boolean matches(String password, String username, String hashedPassword) {
        String encrypted = encrypt(password, username);
        return encrypted.equals(hashedPassword);
    }

    /**
     * 生成随机盐（预留接口，当前使用用户名作为盐）
     */
    public static String generateSalt() {
        return SaSecureUtil.randomUUID();
    }

    /**
     * 生成安全的随机密码
     *
     * @param length 密码长度
     * @return 随机密码
     */
    public static String generateRandomPassword(int length) {
        String chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*";
        StringBuilder password = new StringBuilder();
        Random random = new Random();

        for (int i = 0; i < length; i++) {
            password.append(chars.charAt(random.nextInt(chars.length())));
        }

        return password.toString();
    }
}
```

### 4.7 认证服务

```java
@Service
@RequiredArgsConstructor
public class AuthService {

    private final SysUserMapper userMapper;

    /**
     * 用户登录
     */
    @Transactional
    public LoginResponse login(LoginRequest request) {
        // 1. 查询用户
        SysUser user = userMapper.selectOne(
            new LambdaQueryWrapper<SysUser>()
                .eq(SysUser::getUsername, request.getUsername())
        );

        if (user == null) {
            throw new BusinessException("用户名或密码错误");
        }

        // 2. 验证密码（使用 Sa-Token 加密方式）
        if (!PasswordUtil.matches(request.getPassword(), request.getUsername(), user.getPassword())) {
            throw new BusinessException("用户名或密码错误");
        }

        // 3. 检查状态
        if (user.getStatus() == 0) {
            throw new BusinessException("账号已被禁用");
        }

        // 4. 执行登录
        StpUtil.login(user.getId());

        // 5. 将用户信息存入 Session
        SaSession session = StpUtil.getSession();
        session.set("username", user.getUsername());
        session.set("nickname", user.getNickname());

        // 6. 更新登录信息
        user.setLastLoginTime(LocalDateTime.now());
        user.setLastLoginIp(SaTokenInfoHolder.getSaTokenInfo().getLoginDevice());
        userMapper.updateById(user);

        // 7. 返回结果
        return LoginResponse.builder()
            .token(StpUtil.getTokenValue())
            .tokenName(StpUtil.getTokenName())
            .userInfo(buildUserInfo(user))
            .build();
    }

    /**
     * 用户注册
     */
    @Transactional
    public void register(RegisterRequest request) {
        // 1. 校验用户名
        if (request.getUsername() == null || request.getUsername().length() < 3) {
            throw new BusinessException("用户名长度不能少于 3 位");
        }

        // 2. 校验密码强度
        if (request.getPassword() == null || request.getPassword().length() < 6) {
            throw new BusinessException("密码长度不能少于 6 位");
        }

        // 3. 检查用户名是否存在
        Long count = userMapper.selectCount(
            new LambdaQueryWrapper<SysUser>()
                .eq(SysUser::getUsername, request.getUsername())
        );

        if (count > 0) {
            throw new BusinessException("用户名已存在");
        }

        // 4. 创建用户（使用 Sa-Token 加密密码）
        SysUser user = new SysUser();
        user.setUsername(request.getUsername());
        user.setPassword(PasswordUtil.encrypt(request.getPassword(), request.getUsername()));
        user.setNickname(request.getNickname());
        user.setEmail(request.getEmail());
        user.setPhone(request.getPhone());
        user.setStatus(1);

        userMapper.insert(user);

        // 5. 分配默认角色
        // ... 角色分配逻辑
    }

    /**
     * 获取当前用户信息
     */
    public UserInfo getCurrentUserInfo() {
        Long userId = UserContext.getCurrentUserId();
        SysUser user = userMapper.selectById(userId);

        if (user == null) {
            throw new BusinessException("用户不存在");
        }

        return buildUserInfo(user);
    }

    /**
     * 修改密码
     */
    @Transactional
    public void changePassword(ChangePasswordRequest request) {
        Long userId = UserContext.getCurrentUserId();
        SysUser user = userMapper.selectById(userId);

        if (user == null) {
            throw new BusinessException("用户不存在");
        }

        // 验证旧密码
        if (!PasswordUtil.matches(request.getOldPassword(), user.getUsername(), user.getPassword())) {
            throw new BusinessException("原密码错误");
        }

        // 加密新密码
        user.setPassword(PasswordUtil.encrypt(request.getNewPassword(), user.getUsername()));
        userMapper.updateById(user);
    }

    private UserInfo buildUserInfo(SysUser user) {
        return UserInfo.builder()
            .id(user.getId())
            .username(user.getUsername())
            .nickname(user.getNickname())
            .avatar(user.getAvatar())
            .email(user.getEmail())
            .phone(user.getPhone())
            .build();
    }
}
```

### 4.7 安全工具类

```java
/**
 * 安全工具类 - 从 Sa-Token 获取当前用户 ID
 */
public class SecurityUtil {

    /**
     * 获取当前登录用户 ID
     */
    public static Long getUserId() {
        return StpUtil.getLoginIdAsLong();
    }

    /**
     * 获取当前登录用户 ID（带默认值）
     */
    public static Long getUserIdOrDefault(Long defaultValue) {
        try {
            return StpUtil.getLoginIdAsLong();
        } catch (NotLoginException e) {
            return defaultValue;
        }
    }

    /**
     * 判断是否已登录
     */
    public static boolean isLogin() {
        return StpUtil.isLogin();
    }

    /**
     * 获取当前 Token
     */
    public static String getToken() {
        return StpUtil.getTokenValue();
    }
}
```

### 4.8 需要身份校验的接口列表

**⚠️ 重要**: 以下接口需要用户登录后才能访问，`userId` 从 Token 中获取，前端无需传递。

#### 4.8.1 模拟面试相关接口

| 接口 | 方法 | 改造前路径 | 改造后路径 | 改造说明 |
|------|------|-----------|-----------|----------|
| 创建面试会话 | POST | `/mock-interview/start` | **保持不变** | userId 从 Token 获取 |
| 获取用户会话列表 | GET | `/mock-interview/sessions/user/{userId}` | `/mock-interview/sessions/my` | 返回当前用户的会话 |
| 发送面试消息 | POST | `/mock-interview/sessions/{id}/message` | **保持不变** | userId 从会话关联获取 |
| 获取会话详情 | GET | `/mock-interview/sessions/{id}` | **保持不变** | 校验会话归属当前用户 |
| 结束面试 | POST | `/mock-interview/sessions/{id}/finish` | **保持不变** | 校验会话归属当前用户 |

#### 4.8.2 简历管理相关接口

| 接口 | 方法 | 改造前路径 | 改造后路径 | 改造说明 |
|------|------|-----------|-----------|----------|
| 获取用户简历列表 | GET | `/resumes/user/{userId}` | `/resumes/my` | 返回当前用户的简历 |
| 获取默认简历 | GET | `/resumes/user/{userId}/default` | `/resumes/my/default` | 返回当前用户默认简历 |
| 创建简历 | POST | `/resumes` | **保持不变** | 关联当前用户 ID |
| 更新简历 | PUT | `/resumes/{id}` | **保持不变** | 校验简历归属当前用户 |
| 删除简历 | DELETE | `/resumes/{id}` | **保持不变** | 校验简历归属当前用户 |
| 设置默认简历 | PUT | `/resumes/{id}/default?userId=xxx` | `/resumes/{id}/default` | 校验归属后设置 |

#### 4.8.3 OCR 相关接口

| 接口 | 方法 | 改造前路径 | 改造后路径 | 改造说明 |
|------|------|-----------|-----------|----------|
| 调用 OCR | POST | `/ocr/recognize` | **保持不变** | userId 从 Token 获取，用于计费 |
| 获取 OCR 记录 | GET | `/ocr/records/user/{userId}` | `/ocr/records/my` | 返回当前用户的记录 |

#### 4.8.4 岗位申请相关接口

| 接口 | 方法 | 改造前路径 | 改造后路径 | 改造说明 |
|------|------|-----------|-----------|----------|
| 创建申请 | POST | `/data/applications` | **保持不变** | 关联当前用户 ID |
| 获取我的申请 | GET | `/data/applications` | **保持不变** | 只返回当前用户的申请 |
| 更新申请状态 | PUT | `/data/applications/{id}/status` | **保持不变** | 校验申请归属当前用户 |

#### 4.8.5 AI 聊天相关接口

| 接口 | 方法 | 改造前路径 | 改造后路径 | 改造说明 |
|------|------|-----------|-----------|----------|
| WebSocket 连接 | WS | `/ws/chat` | **保持不变** | 握手时校验 Token |
| 获取聊天历史 | GET | `/chat/sessions/{id}/messages` | **保持不变** | 校验会话归属当前用户 |

### 4.9 接口改造示例

**改造前**:
```java
@PostMapping("/start")
public ApiResponse<MockInterviewSession> startInterview(@RequestBody StartInterviewRequest request) {
    MockInterviewSession session = interviewService.createSession(
        request.getApplicationId(),
        request.getUserId()  // ❌ 前端传递，不安全
    );
    ...
}
```

**改造后**:
```java
@PostMapping("/start")
public ApiResponse<MockInterviewSession> startInterview(@RequestBody StartInterviewRequest request) {
    Long userId = SecurityUtil.getUserId();  // ✅ 从 Token 获取

    MockInterviewSession session = interviewService.createSession(
        request.getApplicationId(),
        userId
    );
    ...
}
```

### 4.10 拦截器配置

```java
@Configuration
public class SaTokenConfig {

    /**
     * 注册 Sa-Token 拦截器
     */
    @Bean
    public SaInterceptor getSaInterceptor() {
        return new SaInterceptor(handle -> {
            // 登录校验：白名单路径除外
            SaRouter
                .not("/auth/login")
                .not("/auth/register")
                .not("/ws/chat")  // WebSocket 单独处理
                .not("/health")
                .check(r -> StpUtil.checkLogin());

            // 权限认证：管理员接口
            SaRouter
                .match("/admin/**")
                .check(r -> StpUtil.checkRole("ADMIN"));
        });
    }

    /**
     * 注册拦截器到 MVC
     */
    @Bean
    public WebMvcConfigurer webMvcConfigurer() {
        return new WebMvcConfigurer() {
            @Override
            public void addInterceptors(InterceptorRegistry registry) {
                registry.addInterceptor(getSaInterceptor())
                    .addPathPatterns("/**")
                    .excludePathPatterns("/error", "/favicon.ico");
            }
        };
    }
}
```

### 4.11 全局异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    /**
     * Sa-Token 未登录异常
     */
    @ExceptionHandler(NotLoginException.class)
    public ApiResponse<Void> handleNotLogin(NotLoginException e) {
        String message = switch (e.getType()) {
            case NotLoginException.NOT_TOKEN -> "未提供 Token";
            case NotLoginException.INVALID_TOKEN -> "Token 无效";
            case NotLoginException.TOKEN_TIMEOUT -> "Token 已过期";
            case NotLoginException.BE_REPLACED -> "Token 已被顶下线";
            case NotLoginException.KICK_OUT -> "Token 已被踢下线";
            default -> "未登录";
        };

        return ApiResponse.unauthorized(message);
    }

    /**
     * Sa-Token 权限异常
     */
    @ExceptionHandler(NotPermissionException.class)
    public ApiResponse<Void> handleNotPermission(NotPermissionException e) {
        return ApiResponse.forbidden("权限不足");
    }

    /**
     * Sa-Token 角色异常
     */
    @ExceptionHandler(NotRoleException.class)
    public ApiResponse<Void> handleNotRole(NotRoleException e) {
        return ApiResponse.forbidden("角色权限不足");
    }
}
```

---

## 五、前端实现

### 5.1 Token 管理

```typescript
// src/utils/auth.ts

const TOKEN_KEY = 'satoken';

export const authUtil = {
  // 获取 Token
  getToken(): string | null {
    return localStorage.getItem(TOKEN_KEY);
  },

  // 设置 Token
  setToken(token: string): void {
    localStorage.setItem(TOKEN_KEY, token);
  },

  // 移除 Token
  removeToken(): void {
    localStorage.removeItem(TOKEN_KEY);
  },

  // 判断是否已登录
  isLoggedIn(): boolean {
    return !!this.getToken();
  }
};
```

### 5.2 API Client 拦截器

```typescript
// src/lib/apiClient.ts

import axios from 'axios';
import { authUtil } from '@/utils/auth';

const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 30000
});

// 请求拦截器：添加 Token
apiClient.interceptors.request.use(
  (config) => {
    const token = authUtil.getToken();
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// 响应拦截器：处理 401
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      authUtil.removeToken();
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### 5.3 登录页面

```tsx
// src/pages/LoginPage.tsx

function LoginPage() {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const navigate = useNavigate();

  const handleLogin = async (e: FormEvent) => {
    e.preventDefault();

    try {
      const response = await authApi.login({ username, password });

      // 保存 Token
      authUtil.setToken(response.data.token);

      // 跳转到首页
      navigate('/');
    } catch (error) {
      toast.error('登录失败', {
        description: error.message
      });
    }
  };

  return (
    <div className="login-page">
      <form onSubmit={handleLogin}>
        <input
          type="text"
          placeholder="用户名"
          value={username}
          onChange={(e) => setUsername(e.target.value)}
        />
        <input
          type="password"
          placeholder="密码"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
        />
        <button type="submit">登录</button>
      </form>
    </div>
  );
}
```

### 5.4 注册页面

```tsx
// src/pages/RegisterPage.tsx

function RegisterPage() {
  const [formData, setFormData] = useState({
    username: '',
    password: '',
    confirmPassword: '',
    nickname: '',
    email: ''
  });

  const handleRegister = async (e: FormEvent) => {
    e.preventDefault();

    if (formData.password !== formData.confirmPassword) {
      toast.error('两次密码不一致');
      return;
    }

    try {
      await authApi.register({
        username: formData.username,
        password: formData.password,
        nickname: formData.nickname,
        email: formData.email
      });

      toast.success('注册成功，请登录');
      navigate('/login');
    } catch (error) {
      toast.error('注册失败', {
        description: error.message
      });
    }
  };

  return (
    <div className="register-page">
      <form onSubmit={handleRegister}>
        {/* ... 表单字段 ... */}
        <button type="submit">注册</button>
      </form>
    </div>
  );
}
```

### 5.5 用户信息 Store

```typescript
// src/store/userStore.ts

interface UserStore {
  user: UserInfo | null;
  isLoggedIn: boolean;
  fetchUser: () => Promise<void>;
  logout: () => void;
}

export const useUserStore = create<UserStore>((set) => ({
  user: null,
  isLoggedIn: authUtil.isLoggedIn(),

  fetchUser: async () => {
    const userInfo = await authApi.getCurrentUserInfo();
    set({ user: userInfo, isLoggedIn: true });
  },

  logout: () => {
    authApi.logout();
    authUtil.removeToken();
    set({ user: null, isLoggedIn: false });
  }
}));
```

---

## 六、API 接口设计

### 6.1 认证接口

| 方法 | 路径 | 说明 | 是否需要登录 |
|------|------|------|-------------|
| POST | `/api/auth/login` | 用户登录 | ❌ |
| POST | `/api/auth/register` | 用户注册 | ❌ |
| GET | `/api/auth/info` | 获取当前用户信息 | ✅ |
| POST | `/api/auth/logout` | 退出登录 | ✅ |
| POST | `/api/auth/change-password` | 修改密码 | ✅ |

### 6.2 请求/响应格式

**登录请求**:
```json
POST /api/auth/login
{
  "username": "admin",
  "password": "123456"
}
```

**登录响应**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "c0c18f20-7b5a-4a3d-8e2f-9a0b1c2d3e4f",
    "tokenName": "satoken",
    "userInfo": {
      "id": 1,
      "username": "admin",
      "nickname": "系统管理员",
      "avatar": null,
      "email": null
    }
  }
}
```

**注册请求**:
```json
POST /api/auth/register
{
  "username": "newuser",
  "password": "123456",
  "nickname": "新用户",
  "email": "user@example.com"
}
```

### 6.3 改造后的接口对比

| 接口 | 改造前 | 改造后 |
|------|--------|--------|
| 创建面试 | `POST /mock-interview/start`<br>参数: `{applicationId, userId}` | `POST /mock-interview/start`<br>参数: `{applicationId}`<br>userId 从 Token 获取 |
| 获取用户会话 | `GET /mock-interview/sessions/user/{userId}` | `GET /mock-interview/sessions/my` |
| 获取用户简历 | `GET /resumes/user/{userId}` | `GET /resumes/my` |
| 设置默认简历 | `PUT /resumes/{id}/default?userId=123` | `PUT /resumes/{id}/default` |

---

## 七、安全设计

### 7.1 密码加密

使用 **BCrypt** 算法加密密码：

```java
@Configuration
public class SecurityConfig {

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

### 7.2 Token 安全

| 安全措施 | 说明 |
|---------|------|
| HTTPS | 生产环境强制 HTTPS |
| Token 前缀 | 使用 `Bearer` 前缀 |
| 过期时间 | 7 天自动过期 |
| 单点登录 | 可选，同一账号只能一处登录 |
| 刷新 Token | 支持续期功能 |

### 7.3 防护措施

1. **登录限流**: 同一 IP 1 分钟内最多尝试 5 次
2. **密码强度**: 最少 6 位，建议包含大小写字母和数字
3. **SQL 注入**: 使用参数化查询
4. **XSS 防护**: 前端输入转义

---

## 八、实施计划

| 阶段 | 任务 | 预估时间 |
|------|------|----------|
| Phase 1 | 数据库设计和迁移脚本 | 2小时 |
| Phase 2 | 后端 Sa-Token 集成 | 4小时 |
| Phase 3 | 接口改造和测试 | 4小时 |
| Phase 4 | 前端登录注册页面 | 3小时 |
| Phase 5 | 前端 Token 管理 | 2小时 |
| Phase 6 | 联调测试 | 2小时 |
| **总计** | | **17小时** |

---

## 九、密码生成工具使用

### 9.1 生成加密密码

在开发或测试环境中，可能需要手动生成加密后的密码。可以使用以下方式：

#### 方式一：编写临时测试代码

```java
public class PasswordGenerator {
    public static void main(String[] args) {
        String username = "admin";
        String password = "123456";

        String encrypted = PasswordUtil.encrypt(password, username);

        System.out.println("用户名: " + username);
        System.out.println("明文密码: " + password);
        System.out.println("加密密码: " + encrypted);
    }
}
```

#### 方式二：使用 Sa-Token 工具方法

```java
// 直接使用 Sa-Token 的 SaSecureUtil
String password = "123456";
String salt = "admin" + "job_tracker_sa_token_salt_2026";

// 简单加密（不推荐生产环境）
String hash1 = SaSecureUtil.sha256(password + salt);

// 多次散列（推荐）
String hash = hash1;
for (int i = 0; i < 1024; i++) {
    hash = SaSecureUtil.sha256(hash + salt);
}
System.out.println("加密密码: " + hash);
```

### 9.2 默认管理员账户

**数据库脚本中设置的默认账户**:

```
用户名: admin
密码: 123456
```

**⚠️ 重要**: 生产环境部署后，请立即修改默认密码！

### 9.3 密码安全建议

| 建议 | 说明 |
|------|------|
| 最小长度 | 6 位字符 |
| 推荐长度 | 8-12 位字符 |
| 推荐格式 | 大小写字母 + 数字 + 特殊符号 |
| 禁止使用 | 常见弱密码（123456, password 等） |
| 定期更换 | 建议 3-6 个月更换一次 |

---

## 十、常见问题

### Q1: 为什么选择 Sa-Token 而不是 Spring Security？

**A**:
- Sa-Token 配置简单，学习成本低
- 项目是单体应用，不需要复杂的分布式认证
- 中文文档完善，社区活跃

### Q2: Token 存储在哪里？

**A**:
- 服务端：Redis（推荐）或内存
- 客户端：localStorage

### Q3: 如何处理 Token 过期？

**A**:
- 自动续期：用户活跃时自动延期
- 重新登录：过期后跳转到登录页

### Q4: 如何实现"记住我"功能？

**A**:
- 勾选记住我：Token 有效期延长到 30 天
- 不勾选：Token 有效期 7 天

---

**文档状态**: 设计中，待审批
