# 简历快照与 JD 快照生成方法完善

## 文档概述

**改进日期**: 2026-03-18
**改进文件**: `MockInterviewService.java`
**改进方法**:
- `generateResumeSnapshot()` - 生成简历快照
- `generateJdSnapshot()` - 生成 JD 快照

---

## 一、generateResumeSnapshot() 方法完善

### 改进前的问题

```java
// ❌ 简单实现，信息不完整
return String.format("{\"work_years\": %d, \"position\": \"%s\"}",
    resume.getWorkYears() != null ? resume.getWorkYears() : 0,
    resume.getCurrentPosition() != null ? resume.getCurrentPosition() : "");
```

**问题**:
- 只包含基本的工作年限和职位
- 没有技能详情
- 没有项目经验
- 没有工作经历
- 手动拼接 JSON，容易出错

---

### 改进后的实现

#### 1. 新增依赖注入

```java
private final ResumeWorkExperienceMapper resumeWorkExperienceMapper;  // 工作经历
private final SkillTagMapper skillTagMapper;                        // 技能标签
```

#### 2. 完整的数据查询

```java
// 1. 查询简历技能
List<ResumeSkill> skills = resumeSkillMapper.selectList(
    new LambdaQueryWrapper<ResumeSkill>()
        .eq(ResumeSkill::getResumeId, resumeId)
);

// 2. 查询项目经验
List<ResumeProject> projects = resumeProjectMapper.selectList(
    new LambdaQueryWrapper<ResumeProject>()
        .eq(ResumeProject::getResumeId, resumeId)
        .orderByAsc(ResumeProject::getDisplayOrder)
);

// 3. 查询工作经历
List<ResumeWorkExperience> workExperiences = resumeWorkExperienceMapper.selectList(
    new LambdaQueryWrapper<ResumeWorkExperience>()
        .eq(ResumeWorkExperience::getResumeId, resumeId)
        .orderByDesc(ResumeWorkExperience::getStartDate)
);

// 4. 批量查询技能名称
List<Long> skillIds = skills.stream()
    .map(ResumeSkill::getSkillId)
    .distinct()
    .toList();
List<SkillTag> skillTags = skillTagMapper.selectBatchIds(skillIds);

// 5. 构建 skillId -> skillName 映射
Map<Long, String> skillNameMap = skillTags.stream()
    .collect(Collectors.toMap(
        SkillTag::getSkillId,
        SkillTag::getSkillName,
        (existing, replacement) -> existing
    ));
```

#### 3. 使用 ObjectMapper 构建 JSON

```java
com.fasterxml.jackson.databind.ObjectMapper mapper = new com.fasterxml.jackson.databind.ObjectMapper();
com.fasterxml.jackson.databind.node.ObjectNode rootNode = mapper.createObjectNode();

// 基本信息
rootNode.put("work_years", resume.getWorkYears() != null ? resume.getWorkYears() : 0);
rootNode.put("current_position", resume.getCurrentPosition() != null ? resume.getCurrentPosition() : "");
rootNode.put("target_level", resume.getTargetLevel() != null ? resume.getTargetLevel() : "");
if (resume.getSummary() != null) {
    rootNode.put("summary", resume.getSummary());
}

// 技能列表
ArrayNode skillsArray = mapper.createArrayNode();
for (ResumeSkill skill : skills) {
    ObjectNode skillNode = mapper.createObjectNode();
    skillNode.put("skill_id", skill.getSkillId());

    // ⚠️ 新增：技能名称（从 skill_tags 表查询）
    String skillName = skillNameMap.get(skill.getSkillId());
    skillNode.put("skill_name", skillName != null ? skillName : "未知技能");

    skillNode.put("proficiency", skill.getProficiencyLevel());

    if (skill.getExperienceYears() != null) {
        skillNode.put("experience_years", skill.getExperienceYears());
    }

    // ⚠️ 新增：最后使用时间
    if (skill.getLastUsedDate() != null) {
        skillNode.put("last_used_date", skill.getLastUsedDate().toString());
    }

    skillNode.put("is_core", skill.getIsCoreSkill() != null && skill.getIsCoreSkill());

    skillsArray.add(skillNode);
}
rootNode.set("skills", skillsArray);

// ⚠️ 新增：工作经历
ArrayNode workArray = mapper.createArrayNode();
for (ResumeWorkExperience work : workExperiences) {
    ObjectNode workNode = mapper.createObjectNode();
    workNode.put("company_name", work.getCompanyName() != null ? work.getCompanyName() : "");
    workNode.put("position", work.getPosition() != null ? work.getPosition() : "");

    if (work.getStartDate() != null) {
        workNode.put("start_date", work.getStartDate().toString());
    }

    if (work.getEndDate() != null) {
        workNode.put("end_date", work.getEndDate().toString());
    }

    workNode.put("is_current", work.getIsCurrent() != null && work.getIsCurrent());

    if (work.getDescription() != null) {
        workNode.put("description", work.getDescription());
    }

    if (work.getAchievements() != null) {
        workNode.put("achievements", work.getAchievements());
    }

    workArray.add(workNode);
}
rootNode.set("work_experiences", workArray);

// 项目经验（完善字段）
ArrayNode projectsArray = mapper.createArrayNode();
for (ResumeProject project : projects) {
    ObjectNode projectNode = mapper.createObjectNode();
    projectNode.put("project_name", project.getProjectName() != null ? project.getProjectName() : "");
    projectNode.put("role", project.getRole() != null ? project.getRole() : "");

    if (project.getDescription() != null) {
        projectNode.put("description", project.getDescription());
    }

    // ⚠️ 新增：职责描述
    if (project.getResponsibilities() != null) {
        projectNode.put("responsibilities", project.getResponsibilities());
    }

    // ⚠️ 新增：项目成就
    if (project.getAchievements() != null) {
        projectNode.put("achievements", project.getAchievements());
    }

    projectNode.put("tech_stack", project.getTechStack() != null ? project.getTechStack() : "");

    // ⚠️ 新增：项目规模
    if (project.getProjectScale() != null) {
        projectNode.put("project_scale", project.getProjectScale());
    }

    // ⚠️ 新增：性能指标
    if (project.getPerformanceMetrics() != null) {
        projectNode.put("performance_metrics", project.getPerformanceMetrics());
    }

    // ⚠️ 新增：是否进行中
    if (project.getIsOngoing() != null) {
        projectNode.put("is_ongoing", project.getIsOngoing());
    }

    projectsArray.add(projectNode);
}
rootNode.set("projects", projectsArray);

String result = mapper.writeValueAsString(rootNode);
```

---

### 输出示例

#### 改进前
```json
{
  "work_years": 3,
  "position": "Java后端工程师"
}
```

#### 改进后
```json
{
  "work_years": 3,
  "current_position": "Java后端工程师",
  "target_level": "MIDDLE",
  "summary": "3年Java后端开发经验，擅长Spring Boot、Redis、MySQL...",
  "skills": [
    {
      "skill_id": 501,
      "skill_name": "Redis",
      "proficiency": "ADVANCED",
      "experience_years": 3.5,
      "last_used_date": "2026-01-15",
      "is_core": true
    },
    {
      "skill_id": 502,
      "skill_name": "Spring Boot",
      "proficiency": "INTERMEDIATE",
      "experience_years": 2.0,
      "last_used_date": "2025-12-01",
      "is_core": true
    }
  ],
  "work_experiences": [
    {
      "company_name": "某科技公司",
      "position": "Java开发工程师",
      "start_date": "2023-01-01",
      "end_date": "2026-03-01",
      "is_current": true,
      "description": "负责后端系统开发和维护",
      "achievements": "提升系统性能30%"
    }
  ],
  "projects": [
    {
      "project_name": "电商秒杀系统",
      "role": "后端开发工程师",
      "description": "负责高并发秒杀模块的开发",
      "responsibilities": "负责缓存架构设计、消息队列优化",
      "achievements": "支持10万+QPS",
      "tech_stack": "Java, Spring Boot, Redis, Kafka, MySQL",
      "project_scale": "团队15人",
      "performance_metrics": "{\"qps\": \"100000\", \"response_time\": \"50ms\"}",
      "is_ongoing": false
    }
  ]
}
```

---

## 二、generateJdSnapshot() 方法完善

### 改进前的问题

```java
// ❌ 手动拼接 JSON，容易出错
snapshot.append("\"job_title\":\"").append(escapeJson(application.getJobTitle())).append("\",");
// ... 更多手动拼接
```

**问题**:
- 手动拼接 JSON，容易出错
- 只包含基本的岗位信息
- 没有薪资范围
- 没有公司信息
- 没有工作地点

---

### 改进后的实现

#### 1. 使用 ObjectMapper 构建 JSON

```java
com.fasterxml.jackson.databind.ObjectMapper mapper = new com.fasterxml.jackson.databind.ObjectMapper();
com.fasterxml.jackson.databind.node.ObjectNode rootNode = mapper.createObjectNode();

// 岗位信息
rootNode.put("job_title", application.getJobTitle() != null ? application.getJobTitle() : "");
rootNode.put("seniority_level", application.getSeniorityLevel() != null ? application.getSeniorityLevel() : "");
rootNode.put("job_type", application.getJobType() != null ? application.getJobType() : "");

// 岗位描述
if (application.getJobDescription() != null) {
    rootNode.put("job_description", application.getJobDescription());
}

// ⚠️ 新增：薪资范围
if (application.getSalaryMin() != null || application.getSalaryMax() != null) {
    ObjectNode salaryNode = mapper.createObjectNode();
    if (application.getSalaryMin() != null) {
        salaryNode.put("min", application.getSalaryMin());
    }
    if (application.getSalaryMax() != null) {
        salaryNode.put("max", application.getSalaryMax());
    }
    salaryNode.put("currency", application.getSalaryCurrency() != null ? application.getSalaryCurrency() : "CNY");
    rootNode.set("salary_range", salaryNode);
}

// ⚠️ 新增：工作地点
if (application.getWorkLocation() != null) {
    rootNode.put("work_location", application.getWorkLocation());
}

// ⚠️ 新增：公司信息
if (application.getCompanyId() != null) {
    Company company = companyMapper.selectById(application.getCompanyId());
    if (company != null) {
        ObjectNode companyNode = mapper.createObjectNode();
        companyNode.put("company_id", company.getCompanyId());
        companyNode.put("name", company.getName() != null ? company.getName() : "");

        if (company.getIndustry() != null) {
            companyNode.put("industry", company.getIndustry());
        }

        if (company.getSize() != null) {
            companyNode.put("size", company.getSize());
        }

        if (company.getLocation() != null) {
            companyNode.put("location", company.getLocation());
        }

        if (company.getDescription() != null) {
            companyNode.put("description", company.getDescription());
        }

        rootNode.set("company", companyNode);
    }
}

// ⚠️ 改进：技能要求（智能解析）
if (application.getSkillsRequired() != null) {
    String skillsRequired = application.getSkillsRequired().trim();

    // 判断是否已经是 JSON 格式
    if (skillsRequired.startsWith("[") || skillsRequired.startsWith("{")) {
        try {
            // 直接解析并验证
            JsonNode skillsNode = mapper.readTree(skillsRequired);
            rootNode.set("skills_required", skillsNode);
        } catch (Exception e) {
            // 解析失败，作为普通字符串处理
            rootNode.set("skills_required", parseSkillsString(mapper, skillsRequired));
        }
    } else {
        // 逗号分隔的字符串，转换为 JSON 数组
        rootNode.set("skills_required", parseSkillsString(mapper, skillsRequired));
    }
} else {
    rootNode.set("skills_required", mapper.createArrayNode());
}

// ⚠️ 新增：职位链接
if (application.getJobUrl() != null) {
    rootNode.put("job_url", application.getJobUrl());
}

String result = mapper.writeValueAsString(rootNode);
```

#### 2. 新增辅助方法

```java
/**
 * 解析技能字符串（逗号分隔）为 JSON 数组
 */
private ArrayNode parseSkillsString(ObjectMapper mapper, String skillsString) {
    ArrayNode skillsArray = mapper.createArrayNode();

    if (skillsString == null || skillsString.trim().isEmpty()) {
        return skillsArray;
    }

    String[] skills = skillsString.split(",");
    for (String skill : skills) {
        String trimmedSkill = skill.trim();
        if (!trimmedSkill.isEmpty()) {
            skillsArray.add(trimmedSkill);
        }
    }

    return skillsArray;
}
```

---

### 输出示例

#### 改进前
```json
{
  "job_title": "后端工程师",
  "seniority_level": "MIDDLE",
  "job_type": "全职",
  "skills_required": ["Java", "Spring Boot", "Redis"]
}
```

#### 改进后
```json
{
  "job_title": "后端工程师",
  "seniority_level": "MIDDLE",
  "job_type": "全职",
  "job_description": "负责后端系统开发，要求精通Java、Spring Boot、Redis、MySQL...",
  "salary_range": {
    "min": 20000,
    "max": 35000,
    "currency": "CNY"
  },
  "work_location": "北京",
  "company": {
    "company_id": 101,
    "name": "某科技公司",
    "industry": "互联网",
    "size": "100-499人",
    "location": "北京",
    "description": "一家专注于电商领域的科技公司..."
  },
  "skills_required": [
    "Java",
    "Spring Boot",
    "Redis",
    "MySQL",
    "Kafka"
  ],
  "job_url": "https://www.example.com/jobs/123"
}
```

---

## 三、关键改进点

### 1. 使用 ObjectMapper 替代手动拼接

| 方面 | 手动拼接 | ObjectMapper |
|-----|---------|-------------|
| **代码量** | 大量字符串拼接代码 | 简洁的 API 调用 |
| **可维护性** | 难以维护 | 易于维护 |
| **错误率** | 高（容易遗漏转义、括号） | 低（自动处理） |
| **可读性** | 差 | 好 |
| **性能** | 一般 | 更优（优化过） |

### 2. 新增技能名称查询

**问题**: ResumeSkill 只有 skill_id，没有技能名称

**解决**:
```java
// 批量查询技能标签
List<SkillTag> skillTags = skillTagMapper.selectBatchIds(skillIds);

// 构建映射
Map<Long, String> skillNameMap = skillTags.stream()
    .collect(Collectors.toMap(
        SkillTag::getSkillId,
        SkillTag::getSkillName
    ));

// 使用
String skillName = skillNameMap.get(skill.getSkillId());
```

**好处**:
- ✅ Agent 能看到技能名称（如 "Redis"）而不是只有 ID
- ✅ 更容易理解候选人技能构成
- ✅ 便于生成针对性的问题

### 3. 新增工作经历信息

**新增字段**:
- 公司名称
- 职位
- 开始/结束时间
- 是否当前公司
- 工作描述
- 工作成就

**好处**:
- ✅ Agent 能了解候选人的职业发展轨迹
- ✅ 可以根据公司规模调整问题难度
- ✅ 可以挖掘项目经验的真实性

### 4. 完善项目经验信息

**新增字段**:
- 职责描述
- 项目成就
- 项目规模
- 性能指标
- 是否进行中

**好处**:
- ✅ Agent 能深入挖掘项目细节
- ✅ 可以根据性能指标评估项目质量
- ✅ 可以验证项目经验的真实性

### 5. 新增公司信息

**新增字段**:
- 公司名称
- 行业
- 公司规模
- 公司位置
- 公司描述

**好处**:
- ✅ Agent 能根据公司类型调整问题
- ✅ 可以了解面试场景（大厂 vs 初创公司）
- ✅ 可以提供针对性的建议

### 6. 新增薪资范围

**新增字段**:
- 最低薪资
- 最高薪资
- 货币单位

**好处**:
- ✅ Agent 能判断薪资水平与能力的匹配度
- ✅ 可以提供薪资谈判建议

---

## 四、性能优化

### 1. 批量查询优化

**问题**: 逐个查询技能名称，N+1 查询问题

**解决**: 使用批量查询
```java
// ❌ N+1 查询
for (ResumeSkill skill : skills) {
    SkillTag tag = skillTagMapper.selectById(skill.getSkillId());
}

// ✅ 批量查询
List<Long> skillIds = skills.stream()
    .map(ResumeSkill::getSkillId)
    .distinct()
    .toList();
List<SkillTag> skillTags = skillTagMapper.selectBatchIds(skillIds);
```

### 2. 查询排序优化

```java
// 项目经验按显示顺序排序
.orderByAsc(ResumeProject::getDisplayOrder)

// 工作经历按开始时间倒序
.orderByDesc(ResumeWorkExperience::getStartDate)
```

### 3. 空值处理

```java
// 所有字段都进行空值检查
rootNode.put("field", value != null ? value : "default");

// 可选字段只在有值时添加
if (value != null) {
    rootNode.put("field", value);
}
```

---

## 五、Agent 使用场景

### 1. 主面试官

```java
// 生成问题时使用
String question = agents.mainInterviewer().askQuestion(context);

// context 包含完整的简历和 JD 信息
// - 候选人工作经历
// - 项目经验（含技术栈）
// - 技能熟练度
// - JD 要求
// - 公司背景

// 示例问题：
// "看到你在简历中提到在'某科技公司'负责过'电商秒杀系统'，
//  这个项目使用了 Redis、Kafka 等技术，能详细讲讲 Kafka
// 在秒杀场景下是如何保证消息不丢失的吗？"
```

### 2. 副面试官

```java
// 决定下一步时使用
String nextStep = agents.viceInterviewer().decideNextStep(context);

// context 包含：
// - JD 要求的技能列表（含技能名称）
// - 候选人技能列表（含熟练度）
// - 已考察知识点
// - 待考察知识点

// 决策逻辑：
// "JD 要求 Redis（精通），候选人声称 ADVANCED，
//  但还未考察过，应该重点验证 Redis 的深度问题"
```

### 3. 评审专家

```java
// 评估回答时使用
String evaluation = agents.evaluator().evaluate(context);

// context 包含：
// - 问题内容
// - 用户回答
// - 候选人技能熟练度（proficiency: ADVANCED）
// - JD 要求的技能级别

// 评估逻辑：
// "候选人声称精通 Redis（ADVANCED），但对集群模式
//  的回答很基础，与简历声称不符，判定为部分夸大"
```

---

## 六、总结

### 改进成果

| 方面 | 改进前 | 改进后 |
|-----|-------|-------|
| **信息完整性** | 2个字段 | 20+个字段 |
| **JSON 构建** | 手动拼接 | ObjectMapper |
| **技能信息** | 无技能名称 | 含名称、熟练度、使用年限 |
| **工作经历** | 无 | 完整经历 |
| **项目经验** | 简单 | 详细的职责、成就、指标 |
| **JD 信息** | 基本岗位信息 | 含公司、薪资、地点 |
| **代码质量** | 易出错 | 健壮可靠 |

### 核心价值

1. **Agent 智能化提升**
   - 主面试官能根据项目经历生成针对性问题
   - 副面试官能精准选择待考察知识点
   - 评审专家能准确评估简历真实性

2. **评估准确性提升**
   - 有完整的技能熟练度信息
   - 有详细的项目经验信息
   - 有工作经历背景

3. **用户体验提升**
   - 问题更贴合候选人背景
   - 评估更有针对性
   - 建议更实用

### 后续优化建议

1. **缓存优化**: 对技能标签进行缓存
2. **异步生成**: 对大数据量的简历使用异步生成
3. **增量更新**: 只更新变化的部分
4. **版本控制**: 添加快照版本号
