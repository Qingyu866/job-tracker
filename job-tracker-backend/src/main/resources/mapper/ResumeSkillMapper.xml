<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jobtracker.mapper.ResumeSkillMapper">

    <!-- 批量插入技能 -->
    <insert id="insertBatch">
        INSERT INTO resume_skills
        (resume_id, skill_id, proficiency_level, experience_years, last_used_date, is_core_skill, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.resumeId}, #{item.skillId}, #{item.proficiencyLevel}, #{item.experienceYears},
             #{item.lastUsedDate}, #{item.isCoreSkill}, NOW())
        </foreach>
    </insert>

</mapper>
